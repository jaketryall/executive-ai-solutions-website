# Decisions — /dark (append-only)

This file is APPEND-ONLY law. Never edit or delete a line below — if a new
step would require reversing one, that is a CONFLICT: stop, do nothing,
and surface it (to the main loop, and to Jake if needed) rather than
silently overriding a prior call.

- 2026-09-06 · seams · five seams get five distinct devices, never the same mechanism twice in a row (law 1) — hero→§02 sticky-cover climb, §02→§03 fade+whitespace, §03→§04 whitespace-only seam + repeated opener, §04→§05 the page's one ground flip, §05→§06 bookend dome mask (ffd1ec9, 7d8f466, 2a9bb7a)
- 2026-09-06 · §02 · surface stays WHITE (#f5f5f7 + original inks) — light gray and true mid gray were both tried and rejected ("the original whiter is better"); do NOT re-propose gray for §02 (bb2627b)
- 2026-09-06 · §02 · service-shot clip-open + parallax removed, shots rest STILL (saturate .25, colour only on hover) — "the animation on the images… drags my eyes to it"; a whole ROW may still grow as one unit under the spotlight and carry its still image along (cdc6f4c, amended 6b886f2)
- 2026-09-06 · §02 · lens+ride: --spot is a raised cosine of the row-centre's distance from the 60vh reading line (reach 1.5× row height, no plateau), chased 0.1/frame; row scales 4.5% from its BOTTOM edge; --dy per row rides the list's lift plus the growth of every row below it — nothing ever moves down (0a2c6a4)
- 2026-09-06 · §02 · row hover removed entirely; the arrow-slide and colour state now ride --spot/is-now instead of :hover (eaf00a2, 0a2c6a4)
- 2026-09-06 · §02 · never grow a large surface via clip-path/inset/radius on scroll — the room-fill light is a CONSTANT box scaled on X only, radius pre-stretched (fb13710)
- 2026-09-06 · §03 · nothing composited at rest inside a translating card — no backdrop-filter or live clip-path'd video while a card is still moving through its entrance; blur is a `filter` on the cover image, hover-only; the demo panel is `visibility:hidden` until hovered (efc50c6)
- 2026-09-06 · §03 · the picture covers only the VISIBLE part of its well, and is NOT chased — well is 3:2, img top -19%/height 126%, translate driven directly off --pp with no lerp of its own; a 0.1 chase's lag exceeded the well's margin at wheel speed (cf7ad97 → 140678b)
- 2026-09-06 · hero · the hero roster keeps its small hover-clips as the through-line motif alongside §03's cards, unless Jake feels it read as a repeat once he reviews (0050b34)
- 2026-09-06 · global · Lenis IS mounted on /dark, via the root layout's SmoothScroll — desktop wheel only, lerp 0.1; no section's scroll math should assume it is unsmoothed or that a local chase is standing in for it (correction noted at eaf00a2)
- 2026-09-06 · §04 · the walking light and the never-settling drift are REMOVED — the grid's window is a one-time ARRIVAL (data-sp-from 1 → 0.62), every panel whole by grid-top ≤.73vh, nothing answers scroll after that point; §04 is the back-half's still beat (2392de6)
- 2026-09-06 · §04→§05 · the page's ONE body-ground flip is declared on `.dr-obj`: void → surface-2 (#171719); the ground holds to the page end and never flips again (2a9bb7a)
- 2026-09-06 · §06 · `.dr-close-rig` has no `data-sp` and no lag translates — §06 is rigid, deliberately (law 12) (2a9bb7a)
- 2026-09-06 · headings · one triggered vocabulary only — `data-wipe` line-wipe on every heading across §03–§06; no second triggered vocabulary gets added without retiring this one first (f01453c)
- 2026-09-06 · §05/§06 · SUPERSEDES the §05→§06 dome-mask seam (line 8) and "§06 is rigid" (line 20), on Jake's ask: the ending is rebuilt to the Off+Brand sequence (design-dna/decodes/offbrand.md) — §05 becomes a WHITE card that recedes (scale only, constant radius, native scroll via data-sp so it runs on touch) to reveal the lifted ground, and §06 is the CTA + footer as ONE section on that ground; the rig/glow/dome/mark bookend is retired. The ONE ground flip (line 19) stands. New seam device = object-recedes-to-reveal-ground (distinct from the other four).
- 2026-09-06 · §05/§06 · shipped numbers for the card-recede + CTA/footer room: `.dr-obj-card` scale window is bottom-edge 0.6→0.3vh, lerp 0.12, scale 1→0.94, radius clamp(22px, 2.2vw, 32px) constant; `.dr-close-h` is sized to its OWN column via `container-type: inline-size` on `.dr-close-say` + `font-size: clamp(2rem, min(1rem + 5.4vw, 12.6cqw), 6.4rem)` — not viewport vw — after the original vw-only formula wrapped 3 of 4 lines at 1440 against the given 7fr column; copy shortened to "How about" / "a site that books" / "for you, [pill]" / "while you work." to fit.
- 2026-09-06 · kickers · NO chapter mark above section titles: the `::before` monogram on the §03/§04 kickers is removed on Jake's call ("the little logos above the section titles don't make sense"); the mark appears only in the nav lockup and the §06 closing lockup
- 2026-09-06 · §04 · SUPERSEDES the staircase (four panels each a tread higher, treads + risers drawn under them) on Jake's call ("the staircase thing is ugly"): the four steps sit in ONE ruled slab — one flat surface, four cells, hairline dividers, one ruled line under the station numbers that lights 01→04 on the arrival; no per-cell lift, no risers, no backdrop blur. The arrival window and per-cell stagger (line 18) stand. (ba6ad74)
- 2026-09-06 · §03→§04 · SUPERSEDES "whitespace-only seam + repeated opener" (line 8) and the §04 window numbers in line 18, on Jake's ask ("something dramatic … motion or movement wise"): the seam is a CURTAIN LIFT — §03 is an opaque black surface that translates up by --seam-lift (32vh, ≈1.5× scroll) as its wrapper's bottom edge crosses 1vh→.32vh, uncovering §04, which is now a LIGHT ROOM (#f5f5f7 foreground plate, §02's ink tokens, white ruled slab) overlapping §03's box by the same 32vh; straight hem; §05's white card straddles the plate's bottom (soft out). §04 still never moves (line 18's still beat stands; its slab window is now .7→.35, whole by grid-top ≤ .46vh). The ONE body flip (line 19) stands. Seam devices are now: climb-over-pin / fade+whitespace / CURTAIN LIFT / ground flip / recede — five, distinct (law 1). (aacb872)
- 2026-09-06 · §03→§04 · the §04 header sits 0.9×--seam-lift below the plate's top so it is the first thing uncovered (p≈.1); the slab has NO fade or rise of its own — the curtain is the only reveal, the ruled line's walk and the demos' one-shots are the section's only motion; §05's card top sits --exit-lap above the plate's bottom edge (a1f006f)
- 2026-09-06 · §04 · header wipes at data-wipe-at .86 — the frame it clears the curtain's hem (b58a76e)
- 2026-09-06 · §03→§04 · SUPERSEDES lines 26–28 on Jake's call ("i really dont like the site changes, revert please", said after seeing the three curtain-lift frames): the CURTAIN LIFT and the LIGHT §04 ROOM are RETIRED — code restored to `40aec08` (§04 on the void, whitespace-only seam of line 8 stands, the ruled slab of line 25 kept). What was rejected on sight = a light room in the middle of the dark room; any future dramatic seam here stays on black.
- 2026-09-07 · §02 · the pictures are PHOTOGRAPHS, not CSS mocks: each stage's real screen was captured, given a drawn iOS status bar in a notch-safe band (`scripts/services/statusbar.py`), and handed to nano_banana_pro at 4k as an `image_references` — the model paints the hardware around a screen it must not redraw (recipe + failure modes in `design-dna/higgsfield/`). The shared `.dvc` / Google / chat skins stay in globals.css for the shipped site's own sections; /dark stopped drawing them.
- 2026-09-07 · §02 · the LANDING stage is a DUO — the desktop build on a laptop with the mobile build on a phone in front of it, both screens real, one generation. It replaced a third phone of the same client's site on Jake's call ("we don't really need the desert wings middle one because we are doing the duo"): the duo already shows that page on two screens, so a phone of it as well was the same evidence twice.
- 2026-09-07 · §02 · the well is the SHOT's own 4:3 on the shot's own dark ground (#1c1a20), not a `clamp()` height on a light #ededf0 tray — a box of a different shape would crop a frame already composed and cropped once, and a light ground under a dark JPEG is a bright hole while it decodes.
- 2026-09-07 · §02 · AMENDS the LENS's LIGHT (the `opacity: .5 + .5·spot` on the whole row, "phones included"): the spot dim now rides `.dr-svc-row > :not(.dr-svc-well)`, and the picture takes the same curve as a dark veil (`.dr-svc-well::after`, #0b0b0d, `opacity: (1 - spot) × .45`). Intent unchanged — mechanism changed because the picture became DARK: opacity fades a thing toward the page, this page is near-white, and the unlit stages rendered at rgb(124,123,126) — pale grey slabs. With the veil the same patch renders rgb(26,25,28). Measured, not guessed.
- 2026-09-07 · §02 · the RIDE's travel runs 1.5 row-heights PAST the list's bottom (`min(listH + hs[last]*1.5, line - listTop) * 0.05`), at the same rate — "nothing ever moves down" was a claim the construction did not keep, and the middle row was measured creeping 6.5px back down over 160px of scroll while its tail was still on screen. Rejected: a max-so-far ratchet on `shown`, which would make the check pass by giving `--dy` a memory of where the scroll had been (law 11 — scrubbed, runs backwards exactly) and would open a gap under a row whose growth had decayed. The documented 5%-by-the-list's-bottom is untouched; the clamp simply lands later, and the list's total ride grows 57.6px → 85.8px.
- 2026-09-07 · NEW SECTION between §03 and §04 · IN THEIR WORDS — a centred claim in the company's own voice, then the client quotes. Deliberately UNNUMBERED in the comments: renumbering §04–§06 for an insert would have churned four files' worth of prose for nothing.
- 2026-09-07 · IN THEIR WORDS · NOT A GRID. The first cut was three quote cards in a row and it read as §03 played twice (Jake: "it might just be the three cards being repeated again too, back to back essentially"). A repeated SHAPE is not rescued by drift or by a ground swap — he had floated a background switch as the fix and the archetype was the real cause. So the axis turns: one centred column, quotes at display scale as statements, no card surfaces. §03 is horizontal and full-bleed; this is vertical and narrow.
- 2026-09-07 · IN THEIR WORDS · each quote declares its own window and the stylesheet folds --sp into a PEAK (`clamp(0, min(sp*2, (1-sp)*2), 1)`) — §02's lens said in the engine's existing vocabulary rather than a second hand-rolled JS pass. Default .5 so a no-JS column is whole and lit.
- 2026-09-07 · IN THEIR WORDS · the centred title is THE ONE EXCEPTION to law 11's single triggered vocabulary (Jake: "the centered text title needs a cool entrance"). Its two sentences pull into focus — blur 12→0, scale 1.05→1, up 18px, 850ms, 220ms apart — off the measured Apple title-card decode. The kicker above it still wipes, so the room's standard language is still spoken in the same header. Rest state is the FINISHED frame, so no-JS never sees an invisible headline.
- 2026-09-07 · §03 · the proof row gets an EXIT as well as an entrance (Jake: "give the proof cards some movement out similar to their movement in, something small when it's almost out of viewport"): each <li> declares a second window off its BOTTOM edge (0.55 → 0.02) writing --ex, and lifts a quarter of its entrance drop in the same two rates (26px outer / 13px centre). It was the only structure in the room with a composed arrival and no leaving.
- 2026-09-07 · IN THEIR WORDS · ⚠ BLOCKED FROM SHIPPING: `lib/quotes.ts` holds PLACEHOLDER quotes (de-named, role-only, invented sentiment, documented as such in that file). Acceptable while they sat small under a price beat; not acceptable as the section itself. Real permissioned quotes with names — and ideally the client's live site linked under each, so a visitor can verify one — must land before this section goes to production.
- 2026-09-07 · TYPE SCALE · the room now locks type the way it already locked spacing. There were ~30 distinct font sizes in dark.css — thirteen between 10px and 17px, and two display clamps differing by .1vw that landed on the same maximum (Jake: "our text sizes are all over the place for the site, i'm worried it makes the site feel disconnected"). Six display rungs + four text rungs, `--fs-*` on `:root`; 59 rules point at them. Only deliberate exceptions stay raw: the nav lockup's responsive steps (10/11px), `.dr-svc-arrow` (17px, an arrow glyph sized optically), the hero's own type and §06's giant wordmark.
- 2026-09-07 · TYPE SCALE · the PEAKS were levelled UP, on Jake's call ("i trust your judgement"): §02's statement 66px and §06's close 102px both join the voices title at 88px. The page raises its voice three times and now does it at one volume. Section headers likewise unified at 43px, from 43 / 38 / 38.
- 2026-09-07 · TYPE SCALE · named `--fs-*`, NOT `--t-*`: `--t-*` is already the duration prefix in this file, and the first cut declared `--t-micro: 12px` in the same rule as `--t-micro: 300ms`. The duration won, and four selectors silently inherited 17px. Declared on `:root` and not `.dr-root` for the same class of reason: `.t-meta` is used by the site nav and footer, which live outside the room's wrapper and could not see a token scoped to it.
- 2026-09-07 · §04 · the section is now a BIG CARD and the card IS the background change (Jake: "we need a big card … for the background change or something … it needs a cool scroll reveal entrance and some sort of background change"). §05 below already flips the page ground void → surface-2, so flipping it here too would be one device twice in a row (law 1) — instead an object arrives and takes the frame: header and all four steps inside a lit panel (gradient surface-2 → surface-1), so the ground changes without the room changing. Still dark; a light room mid-page stays rejected.
- 2026-09-07 · §04 · ONE WINDOW for the whole section. The scrub moved off the <ol> onto the card and `--sp` inherits down, so the card's rise, the grid and the four cells' stagger are the same number — the card cannot arrive out of step with what is written on it. Reveal is transform-only (scale .945→1 from a constant radius, 54px lift): a rounded clip or an inset+radius grow re-rasters the whole panel every frame, a scale is free. Cells retimed to finish within .03 of the card (they ran to .84 against the card's .59 and read as two events).
- 2026-09-07 · §04 · `.dr-runs .wrap` takes §03's 1760px measure rather than the page column's 1290px: at 2560 the shared wrap is half the viewport, and a panel meant to read as the ground cannot be a stripe down the middle of one.
- 2026-09-07 · TYPE · SUPERSEDES the same-day "level the peaks UP" call, on Jake's read of the result ("title text on 2 and 4 looks massive"). §02's statement is NOT a peak — it dropped from --fs-display (88) to --fs-feature (56), a rung below, where it sits with the quotes. --fs-section came back down 43 → 38, which is where two of its four users already were. The lesson worth keeping: level a rung TOWARD the value already carrying most of its weight, not away from it.
- 2026-09-07 · THE FLIP · SUPERSEDES "never a light room mid-page" (2026-09-06). That call was made against a light §04 revealed by a curtain lift and Jake rejected the whole treatment on sight; this is the opposite instruction given knowingly ("i want the whole background to go from black to white and the white aspects to go to black like completely flip"). Between the testimonials and §04 the room inverts: ground black → #f2f2f4 (OFF-white, not paper — his call), and every colour token color-mixed against one --flip so ink, surfaces, edges and hairlines cross together.
- 2026-09-07 · THE FLIP · ONE ELEMENT OWNS THE GROUND. Every bg track writes its target every frame, clamped, so a track resting at progress 0 paints its `from` colour over the whole page above it — pointing §05's at steel turned the entire top of the site steel. §04 and §05 both gave up their ground tracks.
- 2026-09-07 · engine · `data-sp-target` added, mirroring the existing `data-bg-target`: a track can write its var onto a named ancestor. Custom properties only inherit downward, and the page-wide fixed layers (.dr-atmos, .dr-vignette) sit ABOVE the flip wrapper — without this they cannot cross with the room.
- 2026-09-07 · THE FLIP · the two fixed light layers must cross too, and they are separate problems: .dr-atmos is the white lights, .dr-vignette is the BLACK surround. Fading the first left the second, and a black corner-vignette on an off-white ground is a spotlight — Jake: "it looks like i am looking at the sun". The vignette's gradient moved to a ::before so the entrance animation and the flip factor can multiply instead of fighting over one opacity.
- 2026-09-07 · §04 · the four step demos are drawn in white and vanish on a white card; their two inks now color-mix against --flip. The pinned panel centres as a COLUMN and owes the fixed nav clearance — `align-items:center` on a row put the content against the top with a screen of dead space beneath it.
- 2026-09-07 · §02 · the grow STOPS SHORT of the edges (Jake: "i want it to be a card that is almost full screen but has small gaps on the edges"). It used to reach the full 106% box, whose sides sit past the viewport — full bleed with the corners squared off by the overflow. The grown end is now 100vw less a `clamp(12px, 1.6vw, 28px)` gutter either side, so the surface keeps its edges and its radius the whole way and never stops being an object. Only the top of the scale ramp moved; the contained fold state is unchanged (115.2px gutters at 1440, as before).
- 2026-09-08 · IN THEIR WORDS · the quotes go BACK onto cards (Jake: "having each quote in a card and having micro animations inside of them"). This does not reopen the 2026-09-07 rejection: what he rejected was the SHAPE — three cards side by side under §03's three cards side by side — and the column is untouched (one card at a time, full width, vertical). What the card adds is a surface, which is his own standing law (content lives on panels, never bare on the canvas). It is not a box the quote sits in: it MATERIALISES with the light, in three beats off the same --lit the lens already computes — the surface fades up (::before opacity, floor .14 so unlit quotes are still objects), a rule opens from its centre (scaleX), and the attribution rises last (its own ramp past --lit .34). All scrubbed, none triggered. Surface and border live on a pseudo whose OPACITY moves and the rule is a transform, so nothing repaints per frame. List gap 132 → 62px: the card's own padding is now ~110px of the separation.
- 2026-09-08 · §04 · the step cards get a WELL — a little screen recessed into the card, filled with --void (the room's own ground), the same frame on all four. The artifacts used to be drawn straight onto the card face and read as decoration; framed, they read as something being shown, and the row becomes one idea with four contents. Card also gets a shadow under the flip (`.dr-flip .dr-run`): on the void a surface two steps lighter IS the lift, but inverted those two steps are #f2f2f4 → #fdfdfe, ~7 in luminance — the four cards measured as pale rectangles, which is what "I just don't like those cards" was looking at. Light rooms carry depth in shadow, not lightness.
- 2026-09-08 · §04 · FOUR hand-written `rgba(255,255,255,…)` survived the 2026-09-07 crossing (decisions line above) because that fix only touched `.dr-demo`'s two ink tokens: the design blocks' own --d-1 override and its nth-child(1) fill, the growth path's stroke, and BOTH halves of `.dr-run-rail`. All four are white on a white room — the design card measured as an empty panel, the growth line as nothing, and the rail (the card's only structure between the station row and the well) as a gap. The demo inks are now DERIVED from --ink (`color-mix(in oklab, transparent N%, var(--ink))`, three weights) rather than mixed against --flip a second time, so a demo cannot be left behind by a flip again — one source, and the rail derives the same way.
- 2026-09-08 · §04 · the artifacts are redrawn so none of them reads as a LOADING STATE. (a) "design" laid four equal bars in a 2×2 — a wireframe, on the one card whose copy promises you will not get one; it is now page-proportioned (hero band two-thirds, headline + CTA, one short body line) and the hero band has a headline bar and a button inside it drawn in --void, because a solid slab is still a wireframe however well proportioned. (b) "build" was five ragged grey bars = the universal skeleton shape; every row now carries a leading marker drawn in its own background gradient (no markup), which turns the same five bars into a log with entries. (c) "growth" gets gridlines behind the line, because a line without rules to read it against is not a chart. (d) "call" went 13 bars → 41 on a 13-step height cycle: 13 bars spread by space-between across a ~540px well sat 30px apart and read as a barcode; the prime count keeps the cycle from tiling, and the per-bar delay drops 48ms → 18ms so the sweep is one pass, not three.
- 2026-09-08 · THE FLIP · PERF · the flip is PAINT-bound, not style-bound, and the engine now publishes a QUANTISED value for it (`data-sp-step="0.03125"`, 1/32). Measured at 1440 through the flip window: baseline mean 13.6ms/frame, p90 25.0, 28 frames over 20ms and 14 over 33; with `--flip` frozen 8.6/9.3/0/0; with `--flip` STILL MOVING but every token frozen to a literal 9.1ms — so the bare custom-property invalidation is cheap and the whole cost is twelve `color-mix` tokens resolving new colours, which re-rasterises every glyph, border and surface on screen. §04 was innocent (hiding the entire section: 13.2ms — inside the noise), as were the card shadow, the wells and the demos. Stepped: 10.1/16.7/0-6/0 at 1440, and 8.6/8.8/0/0 at 2560, which is the frozen floor exactly. Endpoints stay exact (32/32 = 1, 0 = 0).
- 2026-09-08 · THE FLIP · PERF · the GROUND is deliberately exempt from the step and keeps the unstepped value: it is one composited background-color on one element and freezing it changed frame time by 0.6ms (free), while stepping it WOULD show — #000 → #f2f2f4 in 32 steps is ~8 levels a step across a full screen of flat colour, textbook banding. Colours step, the ground does not.
- 2026-09-08 · ENGINE · a track now skips its write entirely when the value it would publish is byte-identical to the one it last published (keyed on value + viewport height, because the `--<var>x` px twin is derived from vh). A repeated setProperty of the same string still dirties the element, so this is a real saving on every RESTING track, not only the stepped one. The `data-bg` ramp is outside the guard, so the "one element owns the ground" rule (2026-09-07) is untouched. `data-sp-step` must stay 0 for any track whose consumers are TRANSFORMS — a stepped translate is visible stuttering; it exists for colour tracks, which are paint-bound in a way transform tracks are not.
- 2026-09-08 · THE FLIP IS PARKED (Jake: "can we kill the background transition for now"). NOT deleted and NOT reversed — the wrapper, the `.dr-root` token block, every crossed surface in the back half and the atmos/vignette factors all stay exactly as built. What is removed is the TRACK: the nine `data-sp*`/`data-bg-*` attributes come off `.dr-flip`, so the engine never writes `--flip`, every token falls through to its `var(--flip, 0)` default, and the room stays dark end to end — the page as it read before the flip existed. The exact attribute list to restore it is in the comment above that div in page.tsx. This does not settle the 2026-09-07 "SUPERSEDES never a light room mid-page" entry either way; he liked the effect and has not decided where it goes.
- 2026-09-08 · THE FLIP PARKED · consequences, all checked and all fine: (a) §04's cards read on the void as dark panels with black wells and white artifacts — better than they read on the white room, because the well finally has real depth against the card; (b) the `.dr-flip .dr-run` shadow goes inert of its own accord (`calc(.13 * var(--flip, 0))` = transparent), which is correct — it exists because a LIGHT room carries depth in shadow, and there is no light room now; (c) §05's white FAQ card and §06's ending sit on pure void instead of the lifted surface-2 the Off+Brand sequence was designed against, and the recede reads MORE strongly for it (white against #000 rather than against #171719). The back half now has no ground ramp at all — if the ending wants its lifted ground before the flip is re-sited, that is §05's own track to re-declare, never a second one on the wrapper (the one-element-owns-the-ground rule, 2026-09-07).
- 2026-09-08 · IN THEIR WORDS · the lens tent goes ASYMMETRIC with a FLAT TOP (Jake: "each quote section leaves too fast"). At 2 and 2 it was a symmetric triangle touching 1 at exactly one instant (--sp .5), so a quote was fading into or out of the light at every other moment of its pass and never simply WAS lit — that absence of a hold is what reads as leaving too fast, not the exit rate. Now 3.6 in / 2.2 out: arrival ramp --sp 0→.278, held at 1 across .278→.545, departure .545→1. The exit is nearly twice the entry, which is the asymmetry a quote wants — arrive briskly, linger on the way out, because the way out is when you are still finishing the sentence.
- 2026-09-08 · VOICES→§04 SEAM · THE LAST QUOTE CONVERTS (Jake: "i want the last testimonial card to grow downwards as it moves up, text transforms or animates or something and becomes the first card of the horizontal scroll"). One value, `--m`, taken off the --sp the quote already has: opens at .5 (the frame the light starts to go) and completes at .84, so the light leaving and the card converting are ONE event. The card does not dim on the way out — `opacity: calc(.5 + .5 * max(var(--lit), var(--m)))` hands the light the tent is taking away straight to the morph, so it stays lit while it becomes a step card instead of fading to the .5 floor with a step card's face on it. The face is STEPS[0] and the DemoCall component IMPORTED from runs-section, never copied: two hand-kept copies of "01 · Day 1 · the waveform" would drift the first time either is edited, and the entire seam is the claim that they are the same object.
- 2026-09-08 · VOICES→§04 SEAM · the grow is a CONSTANT ::after box of --grow revealed by `scaleY` from its own top — raster-free. Growing the ::before's `bottom` instead would repaint a 1025px rounded box every frame, which is the exact cost the flip was taken off the page for the same day. Measured through the whole column including the conversion: 8.3ms mean, p90 8.4, max 9.4, zero frames over 20. Two fixes the construction needed: the ::before's bottom border and bottom corners interpolate away on --m (or the converted card keeps a rule and a pair of corners across its own middle), and the two layers OVERLAP by 1px (flush at `top: 100%` they left a subpixel hairline of the void between them at fractional scroll positions — a faint rule exactly where the old bottom edge was).
- 2026-09-08 · VOICES→§04 SEAM · the face FILLS the card; it does not sit under it. First cut put it in the opened room at `top: 100%` and the result was a tall empty card with a small step card stuck to its bottom — the quote had gone but the space it occupied had not, so it read as a card emptying out rather than becoming something else. --grow exists only because a step card is taller than a two-line quote. The well is capped at §04's own `clamp(260px, 54vw, 620px)`: left to span this card it stretched to ~1280px against §04's 570 — same waveform, but a strip rather than a screen, and the artifact is the most recognisable thing carrying the seam.
- 2026-09-08 · VOICES→§04 SEAM · ⚠ WHAT THIS IS NOT: a pixel-continuous morph into §04's own card 01. That card is inside a sticky pinned panel whose overflow clips it, and it is 476px of scroll BELOW the point where the quote has already left the top of the screen (measured, 1440) — a literally continuous handoff needs a fixed-position element flown to the pinned card's measured rect and re-measured on resize, which is per-frame rect reading of exactly the kind that just caused the jank Jake reported. This carries the seam on IDENTITY OF FORM instead: same header row, same rail, same waveform in the same size of well, arriving in the same shape. It survives resize, back-scroll and touch and costs two transforms. The widths still differ (1025 here, 620 there) — that difference is a cut, not a morph, and is the piece left on the table.
- 2026-09-08 · VOICES→§04 · the seam is TIGHTENED, and the pixel-continuous flight is ABANDONED. The flight was built (a fixed-position card flown to §04's pinned step card, both rects measured with offsetTop/offsetLeft so the transforms could not feed back) and landed within 0.5px on position and 0 on width — Jake stopped it on sight: "i dont like what you are doing, i liked the previous version better i just didnt like the gap in between". Reverted whole to 35b22c7. What he was actually looking at was 271px of dead ground between the converted card's bottom edge and §04's kicker — 161 of §04's own top seam, 29 of the voices' bottom padding, and 60 more from the quote's exit lift. The real fix was the gap, not the mechanism.
- 2026-09-08 · §04 · `--seam` 22svh → 4svh (`clamp(24px, 4svh, 64px)`; the ≤640 override 16svh → 3.4svh). SUPERSEDES the 2026-09-07 "22svh is a section break, not a chapter of nothing" figure, which was right when §04 arrived cold and silence was the only thing marking the boundary. It is wrong now that the last quote CONVERTS into this section's first step card: the card above introduces §04 rather than ending the testimonials, so a section break's worth of black behind it reads as the page losing its thread. On-screen gap 271px → 139 at 1440, 66 on mobile; the converted card, the kicker and the real card 01 with the same waveform now all land in one frame, which is the whole point of the seam. Pin unaffected (engages at top 0, --sp 0→1, last card's right edge at 1423 of 1440, no horizontal overflow).
- 2026-09-08 · THE FLIP · PARKED note (Jake): when it comes back it goes LATER in the page and it is HORIZONTAL — a sweep, Lando-style — not the global crossfade that was built. The crossfade's token block and the measured perf work (the 1/32 step) stay on the record; the DEVICE changes.
- 2026-09-08 · VOICES→§04 · THE CONVERTING CARD IS STICKY (Jake: "we need to first get the right card sticky, the card that transforms"). Two things had to be true. (a) A flex item's sticky range is bounded by the flex container's CONTENT box — padding or margin on the `<ul>` does not extend it — so with nothing after the last quote its range was zero and it could never hold. `.dr-voices-hold` is an empty final `<li>` of 96svh whose only job is to give the card room to stick through. (b) The stick point IS the end of the morph, by construction rather than by tuning: `--m` is driven by `--sp`, which the engine computes from this card's own rect, so the moment sticky pins the rect `--sp` stops changing and the conversion freezes at exactly the value it had.
- 2026-09-08 · VOICES→§04 · the resting offset is SOLVED, not guessed, because it and the lens's travel depend on each other: the card holds at .155vh, at which point --sp reads .845 and the lens has already lifted it (.5 − .845) × 120 = 41px, so the sticky `top` has to ADD that back — `calc(15.5svh + 41px)`. The first attempt subtracted it and the card came to rest 50px from the top of the screen, under the fixed nav. The 41 is viewport-independent (the lens travel is a fixed 120px), so it holds at every height: 114px at 734, 139 at 900. Frame budget across the whole hold: 8.3ms mean, max 9.3, zero frames over 20.
- 2026-09-08 · §04 · REVERTED the step-01 anchor + lane conveyor (4904ddd → fe869ff) on Jake's call ("yea this looks weird"). The mechanism measured perfectly at its three rest positions and I graded it there — but the section spends most of its scroll BETWEEN them, and a card crossing an opaque anchor is sliced in half: number gone, well cut down the middle, copy starting mid-word. It reads as a rendering bug. Not tunable — it is inherent to holding one opaque card over a moving track. ⚠ THE LESSON: an anchor cannot have things pass BEHIND it. If the anchor idea returns, cards must dim/scale away IN PLACE as the next arrives so nothing is ever half-visible. And grade a scrubbed effect on its travel, not on its beats.
- 2026-09-08 · NEW SECTION between §02 and §03 · WHO BUILDS IT, the split about beat (Jake: "i want to put a split about section like itsjay has … after services … big image on the right with parallax, and on the left the paragraph text"). SHAPE is itsjay's, decoded live: a 12-column grid, copy left at 7 and media right at 5, one big display paragraph rather than a stack of small ones. EFFECTS are NOT his, on his own instruction ("i dont want to copy his exact effects, especially the text one but i do want one"). ⚠ Worth knowing: his media is not parallaxed at all — it is `position: sticky`, holding while the tall text column scrolls past. Ours parallaxes because Jake asked for parallax, and it runs §03's ALREADY-MEASURED rate rather than a second invented one — one parallax rate on the site.
- 2026-09-08 · ABOUT · the text effect is the room's own grammar at a new granularity, not a fourth device: §02's rows and the voices quotes both resolve against a moving light, so the paragraph's ink resolves word by word as it crosses the reading line. Index stands in for vertical position almost exactly inside a single block of copy, so no line measurement is needed and it survives reflow for free. The span is deliberately TIGHT (.16 against a .011 stagger) — about fifteen words in transition at once, a crisp travelling boundary rather than the whole paragraph mushing up together, which is the version of this effect everyone has seen.
- 2026-09-08 · ABOUT · REJECTED an overshoot (the resolving word going briefly brighter than its settled state). There is no headroom: --ink is white on this room, so the only way to fake one is to hold the settled words under full white, which makes a display paragraph permanently dim to buy an effect. The travelling edge is the effect.
- 2026-09-08 · ABOUT · PERF · the words are on OPACITY, not color-mix. Mixing a colour per word per frame re-rasterises 27 runs of 56px glyphs every frame — measured 13.8ms mean with 27 frames over 20ms, the same paint-bound failure the room flip had. Opacity composites and looks identical on a black ground: 10.8ms mean, 1 frame over 20. `data-sp-step` was then tried at 1/64 and moved it 10.8 → 11.0, i.e. nothing — the flip needed quantising because twelve tokens were re-rasterising the whole document; once this was on opacity the paint was already cheap. Removed rather than kept as decoration. Floor is .55 (6.2:1 blended), because an unresolved word is still text somebody may stop on.
- 2026-09-08 · ABOUT · two build bugs worth remembering: React strips JSX whitespace inside a `.map`, so inline-block word spans silently ran the whole paragraph together into one string ("Everysitehereisbuilt…") — the space has to be its own rendered expression. And the first copy draft ran 47 words at display size, making the text column half again taller than the picture and leaving the right half of the section empty below the fold; at this size a sentence IS a paragraph (now 27 words).
- 2026-09-08 · ABOUT · ⚠ THE IMAGE IS A PLACEHOLDER and the section cannot ship on it. There is no photograph in `public/` that belongs in an about beat — every asset is a client work screenshot, and using one says "here is our work" one section before §03 says exactly that with better pictures. Needs a real photograph (the desk, the room, the person).
- 2026-09-08 · ABOUT · the resolve was RETIMED after Jake reported "theres no scroll effect on text" — and it was running perfectly, which is the point worth keeping. Two faults compounded: the stagger (.011 over 28 words) spent only the first 46% of the window, and the window itself (.95 → .15) sat too early. Measured, every word was at full ink by the time the paragraph's top reached .55vh — the whole effect happened while the copy was still in the bottom half of the screen, so by the time it was readable there was nothing left to watch. Now .028 stagger against a .22 span inside a .85 → .05 window: the last word lands at --rp .98, and 7-8 words are in transition at every point of the pass (0 done → 6 → 13 → 19 → 26 as the copy crosses). ⚠ THE LESSON: a scrubbed text effect has to resolve WHILE the copy is in the reading zone; finishing early is indistinguishable from not running.
- 2026-09-08 · VOICES→§04 · the held card is CENTRED (Jake: "i dont like where this sits when its sticky needs to feel centered") — same simultaneous equation as the first solve, aimed lower: it rests at .28vh, where --sp reads .72 and the lens has lifted it 26px, so `top: calc(28svh + 26px)`. Measured held: 248px of air above, 256 below, at 900. And the hold drops 96svh → 35svh: at a full screen the card sat alone with nothing arriving while §04's own header was 837px further down (Jake: "the other cards never appear" — they always rendered, they were a screen late). Now 297px.
- 2026-09-08 · VOICES→§04 · ⚠ OPEN: "theres no introduction to the process section". The sequence is genuinely wrong and shortening the hold only softens it — the card converts into "01 The call" BEFORE anything has said what the four steps are, so the payoff arrives ahead of its own premise. Moving §04's header earlier cannot fix it: to precede the card the header would have to render inside the VOICES section, which is semantically wrong. The real fix is to change what the card converts INTO — see the next entry when it is decided.
- 2026-09-08 · VOICES→§04 · TWO BUGS FOUND BY ACTUALLY WATCHING THE SEQUENCE (Jake: "im thinking you have to actually look at it now and you dont know whats going on" — he was right; every check to that point had been numbers at one scroll position at a time). (1) Centring the card BROKE the conversion. The rule's own comment says "the stick point IS the end of the morph" — --sp freezes at whatever the stick point gives it, so the morph window has to complete at exactly that value. Moving the offset .155vh → .28vh dropped the frozen --sp from .845 to .72 and left --m at .647: the card held on screen at ~48% effective opacity with a half-converted face. The window is now .42/.30 so it lands 1 at .72, and the two are documented as ONE mechanism — move the sticky `top` and this moves with it. (2) STEP 01 RENDERED TWICE: the held card converted into "01 The call" and §04's rail opened with an identical 01 about 200px below it. That is what "the other cards never appear" meant (the first card in the rail was the one you had been staring at) and what "theres no introduction" meant (a numbered step arriving before anything said what the steps were).
- 2026-09-08 · VOICES→§04 · SUPERSEDES "the last quote becomes the first step card" (2026-09-08, earlier): it becomes §04's TITLE instead. The quote lifts out, the surface opens, and "How it runs / Four steps from the first call to a site that earns." fades into the room it made; the card holds that centred, then releases and the rail delivers all four steps. Fixes the duplicate, gives the section its introduction, and keeps 01 in the rail where it belongs — and it is the better idea besides: a client's words becoming our claim rather than a client's words becoming our step one. The face is centred in the GROWN box (`top: calc(50% + var(--grow) / 2)` with a -50% translate) because the surface opens past the card's own height, so 50% of the card is not 50% of what you see; top-aligned it left 176px of empty card under the title.
- 2026-09-08 · VOICES→§04 · §04's own `<header>` STAYS IN THE DOM and is hidden by an INVERTED query — `@media (prefers-reduced-motion: no-preference) { .dr-runs-head { display: none } }`. Deleting it outright would have left the section with no title at all whenever the conversion cannot run (JS off, reduced motion), which is a content bug, not a motion one. The header is therefore the default and motion removes it, so nobody sees it twice and nobody sees none.
- 2026-09-08 · VOICES→§04 · THE LEAD CARD — SUPERSEDES every previous version of this seam (the flight, the sticky hold, the held title card). Jake: "i want the title card to become full size and stick and horizontal scroll it should be the first card in the horizontal scroll it shouldnt travel down". The last quote is no longer in the testimonials column at all: it is the FIRST `<li>` of §04's horizontal track, carrying the quote and step 01 at once. Every earlier attempt failed the same way — a card in that column can only ever hand OFF to §04, because a sticky element's range is bounded by its own flex container. To be part of the rail it has to be IN the rail. Nothing travels down and nothing hands over, because being the first card IS the handoff.
- 2026-09-08 · §04 · the conversion runs on `--lead`, a marker on the stage's top edge (window .35 → −0.5), NOT on the stage's own `--sp` — `--sp` is 0 until the pin engages, so it cannot drive anything that has to happen on the way in. Timing was measured, not guessed: the lead card sits inside the pinned panel, so before the pin its top is always stageTop + 309 and it is only readable as that edge reaches 0. At an earlier 1.35 → 0.1 window the quote had finished fading before the card was even on screen. .35 → −0.5 brings it up from the bottom of the frame with the quote whole, holds it still at the pin, and converts it THERE. One number does the width (quote 1080 → rail 620), the centring offset and the crossfade.
- 2026-09-08 · §04 · the centring shift is on `.dr-runs-card`, NOT the track: `50%` has to mean the column's width, and inside a `width: max-content` track it resolves against the content instead. And the rail's own travel now waits — `clamp(0, (--sp − .25) / .75, 1)` — because the first quarter of the stage belongs to the conversion; without it step 02 slid in beside an unconverted testimonial. The other three cards also fade up on `--lead` past .55, so the rail assembles behind its own first card rather than around a quote.
- 2026-09-08 · ⚠ SOURCE ORDER, THIRD TIME. The touch and reduced-motion fallbacks for the lead card tie on specificity with its base rules, so declaring them in §04's own media query (where they belong topically) lost — the card rendered 260px wide on a phone with the quote still on it. They are now declared AFTER the block they override. Same failure as `.dr-run--anchor` losing `position` to `.dr-run`, and as `--pad` being declared on the track instead of the shared parent. Single-class selectors in this stylesheet are decided by ORDER; put an override after the thing it overrides or give it a parent.
- 2026-09-13 · §01 · THE OPENING — SUPERSEDES the screening (2026-09-12, `2bd8b01`…`b7637cd`). Jake: "i really like the cosmos hero. i like how the video is lower, on scroll the video becomes center screen. the text moves up on scroll out of the way to make room for the video." Cosmos's mechanic, from the decode (`decodes/cosmos.md` §5d) rather than from memory: the statement in the top of the screen, the reel LOW with its bottom on the fold, the stage pinned for 0.66vh while the words rise out and the reel opens UPWARD — height 360 → 648 at 900 (1.80x) at 0.52px per px, width, corners and bottom edge held still. Measured on ours at 1440x900: top 517 → 229, height 360 → 648, bottom 877 constant, rate 0.515px/px, release at 594px; Cosmos 528 → 240, 360 → 648, bottom 888. What survives from the screening: the film is there from the first frame (no grow), the minimal caption (statement · meta line · the one pill; the rail's wordmark is the name), no chip, no sub, no second door. What is gone with it: the recede, the rail's light ink on the film, the name tier. The reel is out of the column to §02's gutter (`--gutter`, 12–28px; Cosmos 16) so the same inset means the same thing twice on the page. The copy's `--chase` stays 1.1 (Jake's own dial from 2026-09-10, against Cosmos's 1.29) — the gap between the labels and the reel opens 0 → 29px and nothing is ever caught. On the phone: no pin, no open, the reel a 4:3 object under the words, and the stage only as tall as its rows.
- 2026-09-13 · §01 · THE LIFT — SUPERSEDES the Cosmos shutter of the same morning (`da61f3c`). Jake, on the shutter: "i really like the big text, and i want to find a different mechanism for the video reveal. i want nothing to feel empty." Three mechanisms were offered (the lift · the dock · the zoom-through); he chose the lift. The film is a full-viewport layer anchored to the fold, UNDER the first screen from the first frame; the room's ground carries the statement and the label row over it with its edge at 60svh, so the film shows in the bottom band at rest (Cosmos's 40% share, kept). Scroll lifts the ground 1:1 — `--lift = --lead × 60svh`, `--lead` completing at 0.857 of a 0.7vh window = 0.6vh of scroll — and the words ride the edge up and out while the film is uncovered edge to edge; it holds full screen for 0.1vh, then the pin releases. ⚠ Mechanically NOT the shutter: the shutter grew a box and let `cover` re-fit the footage every frame (the picture swelled); here only the box's top edge moves and the footage is pinned to the fold at 100svh — more picture is uncovered, none of it scaled. No radius: it is the screen, not a card. Measured at 1440x900: edge 540 → 270 → 0 at scroll 0 / 270 / 540, statement 176–423 riding it, release at 630. Then, at 89px, Jake: "the top half feels so empty" → the statement FILLS THE MEASURE: `--hero-fs` raised until the existing 9.2cqw cap governs (118.8px at 1440, line two exactly the 1290px column), two lines 247px of the ground's 457. `.dr-main { padding: 0 }` had also silently killed `.wrap`'s inline padding (the statement was flush left at 1440 and 390) — `padding-block` only. Phone: no pin, no lift, the film a 4:3 object after the words (the DOM puts it first so it can be under; `order` puts it last).
- 2026-09-13 · §01 · THE LINE — SUPERSEDES the stage (`d5daaf3`, never rendered), the lift (`9cd34bc`) and the shutter (`da61f3c`), all of today. Jake, with a HeartBloom landing (dribbble 27394246): "can we try this but instead of that image in the card its my showreel and we have the itsjay effect for the video grow. i want a 5 star review thing somewhere too and we would be the white background version of course." One centred statement (86px at 1440, `--hero-fs` sized so "systems that [card] follow up." fits the measure on one line) with the REEL inside its second line as a 16:9 card 0.94em tall; the meta line and the one door under it; a rating card lower right. THE GROW is itsjay's (decodes/itsjay.md §5.3): the reel's real home is `.dr-dock`, a full-width block right after the hero (1394×784 at the room's gutter, radius 24 — by 0.9vh of scroll its top is at 0.1vh so it fills the screen on the frame the grow ends, itsjay's 1440×810 geometry); at rest it is transformed onto the slot by three MEASURED numbers (page.tsx: `--s0` = slot height / reel height, `--dx`/`--dy` = the vector between their centres, both read through the offsetLeft/Top chain — ⚠ never getBoundingClientRect: the slot's rect carries the line's entrance transform and the reel landed 93px low for it) and `--p` (0→1 over the wrapper's first 0.9vh, on the WRAPPER because the words and the reel are in sibling subtrees) walks translate to 0 and scale to 1 linearly. Transform only; the radius is divided by the scale so it reads 24px at every size. No pin: the words leave at scroll's rate and fade across the first 60% of the grow. The dock is z 5, above the stage's 4, so the reel grows OVER the words. Measured 1440×900: on the slot at 0 (144×81), 561×315 at 270, 977×550 at 540, docked 23/90/1394/784 at 810, radius 24. Phone: no slot, no grow, the reel is the block after the hero. ⚠ THE RATING CARD IS ON PLACEHOLDER NUMBERS (`lib/proof.ts`: 5.0 · 14) so it can be seen — it cannot ship until Jake supplies the real Google rating, count and profile link; `count: 0` hides it.
- 2026-09-13 · §01 · THE LINE, REVISED (Jake, seeing it: "need to find a way to do the video bigger and maybe a small description as well like acquisition.com"). The reel stops being a word: the slot is a 16:9 CARD under the words that takes the rest of the first screen (flex: 1 in the column, bottom on the gutter above the fold — 765×430 at 1440×900, 53% wide, s0 .48 against the dock), and the words stack tight above it: the meta line, the statement (`--hero-fs` now svh-led, 77px at 900 tall, because it is the HEIGHT the words and the reel divide), a one-sentence description at body-s (the band hero's old sub, kept: it says what the two things are), the door. acquisition.com's hero is headline → one line → pill → the media, measured live 2026-09-13; that order is this. The slot's surface goes transparent once the reel is measured onto it (`.dr-hero-wrap[data-ready]`) — it showed as an empty card the moment the reel left for its dock. Same grow, same dock, same rating card.
- 2026-09-13 · NEW SECTION under the reel's dock · THE RECEIPTS (Jake: "i know acquisition does a lot of analytics on their site so i want to find a way to do similar things but make it look significantly better … a similar idea just look significantly better"). acquisition.com's row, measured live: four figures (10,000 / 81 / 97.1% / 12,000), caps labels, hairlines, no window, no source. Ours: four RECEIPTS from `lib/proof.ts`, every one already stated on a case page or the pricing page — 1,000+ page views (Desert Wings, since launch) · $0 platform fees (AAHG, every month since) · 2 days to a fixed quote (every project) · 60 s to a real price (the estimator) — each with a WINDOW and a SOURCE that links to its proof, an "as of" line, the figures ROLLING IN on the estimator's own odometer once the band enters the reading zone (one arrival per section), separated by space not lines, the numeral on the display face upright and tabular, the value ~4× its label. ⚠ "1,000+" is round because the exact GA count was never pulled (the 2026-08-08 note stands); the band is built to take the exact number. THE NEXT STEP if Jake wants it truly "analytics": the GA4 Data API behind `lib/proof.ts` so the figures are live and dated by the request — needs his GA credentials; nothing on the page pretends to be live until it is.
- 2026-09-13 · §01 · THE BEAUTY PASS (Jake: "theres got to be a way to make it more beautiful"). A three-lens judge panel (type & editorial · imagery & atmosphere · objects & composition) proposed 17 moves; a critic scored them against §3 and this file and kept six, built as one step: (1) THE CHIP MOVES INTO LINE ONE — "Design that [reel] sells," / "systems that follow up" — so the two lines measure within 4% (990 vs 1031px; they were 61% of each other with the chip and the period both loading line two) and the eye enters at the chip and reads forward; NO PERIOD on a display line; the chip set to the line's own extents (.9em, baseline −.18em, 6px radius, the reel's --r0 matching). (2) THE STATEMENT FILLS ITS MEASURE: `--hero-fs: clamp(2rem, min(7.4vw, 14svh), 6.75rem)` → 106.6px at 1440×900, 103 at 1440×736, the longest line ~80% of the column; tracking −0.034em; `--hero-lh` 1.04 (not 1: Archivo's content area is 1.099em and line-height 1 clips the descenders and the chip's bottom edge under .dr-line's overflow:hidden). (3) NO KICKER — the meta line "Websites · Automation · Google Ads" above the H1 was the badge-above-H1 tell and named Google Ads, which the 2026-09-10 positioning line dropped; if the three service names belong on the first screen they go INTO the lead sentence, never above the statement. The lead steps up to reading size (18px/1.62), gaps are the objects' own margins (fib-3, fib-4), not one number between everything. (4) THE WORDS LEAVE IN ORDER: the door and the lead by p .18, the claim and the card by p .22 (178px at 900) — before the claim's top reaches the rail's hem at p .20, so no ghost ever straddles the pill; the sentinel `.dr-top` is 19.8svh tall so the rail closes on the same frame (one door on screen at any moment); entrances lead → card → door (animate-last = highest), all `backwards`; `@keyframes dr-fadein` now declares `from { opacity: 0 }` — with only a `to` every caption fade had been a no-op; reduced motion holds all four at 1 (the engine publishes --hero-p as 1; they sat at 8%). (5) THE DOOR IS THE INK OBJECT: an ink pill with the ground as its label (16:1) — the extreme value is the action, inverted for the light room; hover steps to --ink-2, no hue. (6) ONE SHADOW: `--lift` (the rating card's recipe) as the token, and the reel takes it through `.dr-hero-shade`, a sibling whose box and 24px radius never change so it paints once and only transforms — never on the reel itself, whose radius is recomputed every frame. Dropped by the critic, on the record: a lit ramp on the ground (Mach bands + the cheap-texture overlay), a lit mark (the watermark tell), a photographed wall (HeartBloom's design, not the cool flat ground of 2026-07-17), a second zoom inside the chip (the shutter's fault), a counterweight card lower-left (the floating-pill-pair template), an Archivo-caps eyebrow (badge-above-H1).
- 2026-09-13 · §01 · THE CHIP IS A DROP CAP (Jake, after the beauty pass: "hero still just feels kinda empty, video is hard to see too"). At a word's height the film was a 96px thumbnail — the visibility and the emptiness were one problem: the film was too small to be the hero's object. The h1 is now a ROW: the chip first, two lines tall (the row stretches it, 16:9 → 340×191 at 92px), the statement set beside it left-aligned, the whole row centred — the reel is the title's initial, 2.3× its inline size, and the headline block runs 1257 of the 1290 measure. `--hero-fs` re-sized to the row (chip 3.7em + gap .3em + longest line 9.8em = 13.8em → 92px at 1440); rest radius 10px. Square, no skew: an object beside the type, not a letter in it. Same grow into the same dock from the new position. Phone: the slot hidden, the h1 a centred block. ⚠ WHAT REMAINS OF "EMPTY" IS THE GROUND, not the words: the critic dropped every atmosphere move that was not a tell as an asset question (a photographed ground) — that is Jake's call, and the two honest options are on the table: a light photograph (his, or generated) or the film itself veiled as the wall with the chip as the sharp cut in it.
- 2026-09-13 · §01 · THE VEILED CARD (Jake, with the Hyperactive pizza shot, dribbble 25319896 — a headline across the top of a big picture with copy ON it: "im thinking the video could be dimmed so you get the text on top of it and when you scroll it grows and undims"). SUPERSEDES the drop cap (`b3ea6c2`) and the chip in the title before it. The statement is centred at the top (106px at 1440, `--hero-fs` = min(7.4vw, 12svh)); the CARD — the reel's rest position — takes the rest of the first screen (flex: 1, 16:9 → 1074×604 at 1440×900, 75% wide) and rises under the headline by .55em so the second line crosses its top edge; the description sits lower left ON the card, the door lower right, the rating a badge top right below the crossing. THE VEIL: `.dr-hero-reel::after` is the ground colour at .74 × (1 − p) — in a light room "dimmed" is WASHED toward the cream, not darkened, so the ink on the card reads; it lifts with the grow, 0 when the reel is the screen. One composited plane whose opacity moves. z-order: the stage is 6 and the dock 5, so the reel paints under the words and the card's copy, over the ground (the stage has no background). Phone: the card is a plain block whose words flow, the reel the block after it, no veil. Reduced motion: the reel is docked and the card keeps its own surface under its words. ⚠ The veil's .74 is a dial: on a film cut on black the card at rest is a mid-grey slab with the picture faint in it; lighter shows more film and costs the copy contrast.
- 2026-09-13 · §01 · TO THE REFERENCE (Jake, on the first veiled cut: "it doesnt really look like the reference", then "i want the films proportions to be closer to the reference"). The Hyperactive shot (dribbble 25319896), read properly: a huge condensed ONE-LINE uppercase headline the width of the frame; a dark picture with LIGHT writing on it (a small caps heading + short copy lower left, a button lower right, three sprayed tags across the picture); the picture a LETTERBOX ~2.6:1, nearly the frame's width, under the headline with a small gap — the pizza cut-out is what climbs over the letters, not the picture's rectangle. Built to that: "DESIGN THAT SELLS" in Archivo wdth 66 / 800, uppercase, `--hero-fs` = min(12.8vw, 20svh) → 180px at 1440, filling the 1290 measure (the old 9.2cqw cap on .t-hero removed — it held the line at 132); "Systems that follow up" becomes the card's caps heading; the film DIMMED DARK (`.dr-hero-reel::after` black at .5 × (1 − p), lifting with the grow — the light-room "wash" of the morning was the opposite of the reference and is gone); the three service names as the tags in the room's display face (`.dr-tag`, hand-placed, `text-shadow` for lift on bright frames), the copy block lower left, the WHITE pill lower right (the only pure white on the first screen), the rating badge top right. THE CARD is a 2.4:1 letterbox the full measure wide (1290×538 at 1440×900), pinned to the fold by `margin-top: auto`; THE DOCK takes the same proportion (1394×581) — a uniform scale cannot change a box's aspect — padded on top so the docked reel is CENTRED in the viewport on the frame the grow ends (itsjay's 0.9vh), no longer filling it. The headline's crossing survives as a mechanism (`.dr-line--film`, a white copy clipped to the film's box by three measured numbers) for any viewport where the card meets the letters; a blend mode was tried and rejected — `difference` inverts on a bright frame, the reference never inverts. Verified in real Chrome at 1440×900 and 390×844 (the pane cannot paint video): video playing, scrim .5 → .31 at 300 → 0 at 810, headline gone by 300, no overflow, no console errors.
- 2026-09-13 · §01 · Jake: "in the reference the title is the same width as video, also i dont like the tags get rid of them." THE TAGS ARE GONE (the sprayed Websites / Automation / Google Ads on the film — one look, cut). THE TITLE IS THE FILM'S WIDTH: the hero column is a size container and `--hero-fs: min(14cqw, 20svh)` — "DESIGN THAT SELLS" at wdth 66 / 800 runs 7.146× its size (measured 1286.3px at 180), so 14cqw (100 / 7.146) makes the line exactly the measure, which is the card's width; one number governs both. Measured 1440: title 1286 wide at x 77, film 1290 at x 75 — the 2px each side is the glyphs' own side-bearing. ⚠ RE-MEASURE 14cqw IF THE COPY OR THE WIDTH AXIS CHANGES.
- 2026-09-13 · §01 · WIDER, AND IN CAPS (Jake: "can we make video and title wider, also i really love their look and their all caps reads more creative than professional which is what im going for with this site"). The hero column leaves the measure for the room's gutter — `.dr-main.wrap` overrides the measure here only — so the title and the film are 1394 of 1440 (97%, the reference's ~94%); the height rule loosens to /.5 so a short screen buys width by letting the card run a little past the fold (1440×736: 1193 wide, the card 16px under the fold, the door 38px above it). THE GROW STAYS A GROW: the dock is now 108vw, bleeding 4vw off each edge (the wrapper is `overflow-x: clip`), so the reel still grows 12% at 900 tall and 30% at 736 on its way to the centre; docked at 1440×900 it is 1555×648, centred at 450, no horizontal scroll. CAPS IS THE SITE'S REGISTER: the card's heading in the title's condensed face, the card's copy in small tracked caps (Archivo 600, 13px, .06em), and the rail's links in caps (a sentence-case nav over a caps hero read as two sites). The description's copy is unchanged.
- 2026-09-13 · §01 · ALIGNMENT IS INK, NOT BOXES (Jake: "the company title isnt left aligned with title, the title isnt perfectly aligned on left side, i want right side nav to be aligned with right side of title and video"). Verified in real Chrome by reading the pixels of the frame (PIL over a DevTools screenshot), not the boxes: (1) the title's line was `display: inline` — a span with a block child splits into anonymous line boxes each carrying a full line-height strut, and the one-line title measured 361 tall with 181px of nothing above it; `.dr-line` is `display: block` now. (2) `.dr-line`'s −4° shear (the old statement's) applied to the condensed line — a slanted stem can never sit on an edge; the hero line is upright (`transform: none`). (3) A glyph's box carries a side bearing: at 196px the D's ink began 9px inside its box. The line is pulled left by −0.041em and sized 14.12cqw so the INK spans the column — title ink 24→1416 against the film's 24→1416. (4) The rail's row had a 34px gap before the collapsed action, leaving the links short of the edge: gap 0, the gap arrives with the pill; the links carry −2px for the G's bearing (ink to 1416); the wordmark's E lands at 24. ⚠ THE NUMBERS ARE FOR THIS FACE AT THIS SIZE — re-measure the bearings if the copy, the face, or the width axis changes.
- 2026-09-13 · §01 · THE COPY COMES OFF THE FILM (Jake asked which was better — copy on the dimmed film, or the film below the title with the copy above it — and said "yes" to the recommendation: above). The first screen is now: the title the full measure; a ROW on the ground under it, aligned to the film's edges — the caps heading and the small-caps description left, the ink pill right; then the film, full width, its top in the first screen and its BOTTOM PAST THE FOLD (Cosmos's low video: 300 of its 536px visible at 736 tall, 93px past the fold at 900), UNDIMMED from the first frame (the scrim is gone — it existed only to keep copy legible on the picture), the rating badge the one thing floating on it. This answers "everything's too narrow" by geometry: with nothing written on the film it needs no height of its own in the first screen, so the column is the full measure on every screen and the height rule of the morning is gone. The grow on scroll pulls the film up to the centre, 108vw at the dock. Measured in real Chrome at 1440×736: title 23→1417 at 196px, row 286–411, film from 436, docked 1555×598 centred at p 1 with the row at 0 and the rail stuck.
- 2026-09-13 · §01 · THE PANEL (Jake: "can we try a white card in between the title and video that houses the description and such" → "it might be too strong, like all my attention is there" → "so much wasted space in the middle of it"). The row under the title is a PANEL: `--surface-1` (#f4f4f7 on the #e7e7ea ground), no shadow — a pure-white card with the badge's shadow was tried and read as a spotlight competing with the film. Three columns: the caps heading and the small-caps description at the left; THE OFFER in the middle — Websites · Automation · Google Ads, each with lib/services' own one-liner (`SERVICE_META[k].line`) and each a link to its page, the meta line that left the top of the page given real content; the ink pill at the right. Content on a surface, the surface quieter than the film, no air left in the middle.
- 2026-09-13 · §01 · THE STRIP (Jake's alternative to the panel: "a thin strip as white where some small things are there, like reviews with the little half circle things on far left, just stuff that doesn't take up much vertical space" · "the little avatar icons on left that rotate, that are images of real people, and to the right of that an infinite logo marquee" · "book the call floating beneath"). SUPERSEDES the panel (`def30b4`, kept in git). A 64px white pill-strip between the title and the film, the badge's shadow: at the left three overlapping FACES that rotate (each takes a third of a 7.2s cycle at the front, one transform each; initials RR / JJ / TB — real clients — until photographs are cleared, `lib/proof.ts REVIEWERS`), the stars and the rating; the rest of the strip a seamless CLIENT MARQUEE (wordmarks in the site's caps, faded at both ends; `lib/proof.ts CLIENTS`; only Desert Wings' logo file exists, so wordmarks for all three until the others do). The door floats beneath the strip at the column's left, an ink pill with the room's shadow. The description is off the first screen (About says it next). The badge on the film is gone — the rating lives in the strip. Reduced motion: the faces hold, the marquee stands. Measured 1440×736: strip 278–342, door 358–403, film from 422 (314px of it in the first screen).
- 2026-09-13 · §01 · THE STRIP, SWAPPED (Jake: "instead of companies it would be smarter to have the services in infinite marquee because we can rotate icons inside of the circle things for businesses"). Agreed, and for a reason worth keeping: a "trusted by" marquee with three names loops the same three every few seconds and advertises how short the list is; a services ticker reads as what is done, and it carries the real things inside the services (`lib/proof.ts TICKER`: Websites · Google Ads · Automation · Booking forms · Follow-up texts · Live chat · Conversion tracking · Fixed quotes — every one an item the estimator or a service page already names). The circles are the BUSINESSES the rating comes from (`CLIENT_MARKS`), rotating — the proof beside what it validates — a mark file where one exists (Desert Wings), initials (AA, RP) until the others are cleared. Faces of people are retired: the reviewers are businesses.
- 2026-09-13 · §01b · THE RECEIPTS ARE CARDS IN A TRAY (Jake: "by the numbers each number should have its own white card like the hero thing" → "maybe additionally they should all be in a big card?"). Four white cards — the strip's surface — inside one tray a step lighter than the ground (#f2f2f4) that carries the --lift; the cards carry only the lift's near shadow so they read as tiles on the tray, not four more lifted objects. The head moved inside the tray, inset by the card padding so the kicker's ink sits on the first figure's left edge (measured x=91 both). Tray inset 10px so the corners nest concentrically (24 outside, 14 inside). The band now shares the hero's column edge for edge (55→1385 at 1440, the strip's exact edges); the figure is sized off the card with cqw (25.5cqw of the content box — "1,000+" clears the padding by 28px where at 84px it ran past the card). Frames: design-dna/frames/receipts-cards-2.jpeg. OPEN: every section below still sits on the old .wrap column (x=75 at 1440), 20px in from the hero's — decide whether the whole page moves to the hero's column.
- 2026-09-13 · §01b · THE TRAY IS INK, THE HEAD IS OUT (Jake: "the card being black and the title and as of september not being in it"). Tray = var(--ink) + --lift, cards white with no shadow of their own; "By the numbers" / "As of" back on the ground above it, inset so the kicker's ink sits on the first figure's edge. Inset dialled 10 → 22px: at 10 the black read as a thick stroke around four white boxes (frames/receipts-cards-3), at 22 as a surface the tiles sit on (receipts-cards-4); outer radius = 14 + inset so the corners nest. 1b83d8c is the 10px version if Jake prefers it.
- 2026-09-13 · §01b · ONE WHITE CARD, FIGURES BARE (Jake's third call: "by the numbers and the as of september being in the card, but card is white and each number does not get its own card"). The strip's surface at panel size with the --lift, head at the top inside it, the four figures on the surface separated by space. One inset (37px at 1440) for head and figures: kicker's ink at x=92 = first figure's left edge; "As of" right edge 1348 = last column's. Card 55→1385, the strip's edges. Frame receipts-cards-5. The black tray is 385416b, the four-tiles-in-a-tray 1ba8f96, if either is wanted back.
- 2026-09-13 · §01 · GROW, HOLD, SHRINK (Jake: "the video section has to grow to full and then shrink"). A second window on the dock's own top edge (0 → −0.35vh, var --q): the reel holds at 108vw for 0.1vh after the grow ends, then comes back over 0.35vh of scroll to THE COLUMN — the hero's, the rail's and the numbers card's edges (55→1385 at 1440, measured) — bottom edge held by a translate of (1−s)·h/2 so the film settles onto the card instead of opening a gap. --s1 = column / dock-w via tan(atan2()). Reduced motion publishes q=1: the film sits in the column. Frames: shrink-mid, shrink-end.
- 2026-09-14 · §01 · THE ENTRANCE, WITHOUT THE FLASHES (Jake: "theres a lot of flashes and stuff that happens on page reload"). Screencast of a reload measured three: (1) the DARK room painted first for ~90ms because `.dr-light` was decided in an effect from a default of false — light is now server-rendered, `?dark` flips it off in the effect; (2) the strip and the door were fully visible in that first frame, hidden by the entrance animation's backwards fill at hydration, then faded in — visible → gone → back; they are opacity 0 until `.dr-lit`; (3) the slot's plate was --surface-1 (#f4f4f7 in the light room) and the reel faded in over it while it faded out — a pale rectangle, then the ground bleeding through the crossfade (film region 28 → 73 → 30), then black when the poster painted. The plate and the reel are now the film's black in either room, the reel is simply opaque under the plate once measured, and the plate fades to clear from 240ms — black becomes the poster, region flat at 28–30 the whole way. Clock tightened: rail 140 → reel 240 → title 300/420 → strip 560 → door 720. Frames: reload-sheet (before) / reload-sheet-2 (after).
- 2026-09-14 · §01 · THE FILM RISES AND GROWS (Jake: "video needs cool entrance … like a rise and grow maybe"). The black plate from the morning is gone (a plate that is there at first paint cannot then enter without a jump); the film's place is the ground until the reel is measured, then it rises from 16% below at 88% of its size on the structure curve, opaque by half way, 900ms from 400ms — 100ms behind the title's own rise, the shade on the same keyframes. `transform` composes after the scroll's `translate`/`scale`, so the entrance rides on the grow math untouched. Screencast: film region 230 → 32 monotonic, no bleed. Frames: reel-rise-sheet.
- 2026-09-14 · §01 · THE GROW KEEPS ITS TOP EDGE WITH THE WORDS (Jake: "i love the motion but also kinda dont. it creates blank empty space of white while we scroll … the stuff above it cant disappear so early or the effect has to change"). Both, minimally: (1) the vertical travel is p² — the reel hangs at the slot's height while it grows and drops to the centre at the end, so its top edge rises at ~1.06px per px of scroll with the strip instead of 0.44 (measured: the rail-hem→film gap at mid-grow 245 → 49px); (2) no early fade — the words leave at scroll's own rate, Cosmos's way, and fade only in the rail's zone (title p .06–.20, strip .28–.42), because a crisp headline half behind the half-darkened pill was the ugliest frame. Frames: grow-sheet (no fade at all — the straddle), grow2-sheet (as shipped). scripts/scroll-frames.mjs is the tool.
- 2026-09-14 · §01 · FULL SCREEN (Jake: "is there any way to make the video go full screen as part of the scroll effect"). Yes, without a pin, a clip-path or a layout change: the reel is a MASK the column's letterbox shape, scaled non-uniformly to the viewport's shape, and the picture inside is the whole 16:9 frame counter-scaled so on screen it is always uniform — at rest the letterbox shows the middle band (the crop object-fit gave), at p 1 the mask is the screen and the frame has opened to nearly the whole source. The box is written in the VIEWPORT'S frame, an edge law each: the top follows the strip up at scroll's rate, holds at the rail's hem, walks the last 80px from p .5; the bottom runs from the slot's bottom to the fold linearly — scale and translate are derived (tan∘atan2 divides lengths). Then 0.1vh HELD full screen and 0.35vh shrinking IN PLACE to the column, both by one trick: the dock's top padding puts the reel's flow box at the viewport's centre at 1.35vh, so dy·(1 − q) never moves the centre. Every number a transform; runs backwards exactly; reduced motion = the column. The reel's flow box is the column now (the 108vw letterbox and --s0/--dx/--dy are gone), so the numbers card follows it in flow. Measured at 736 and 900 (checks.md). Frames: full2-sheet.
- 2026-09-14 · §01b · THE NUMBERS BAND IS CUT (Jake: "i think it works for acquisition.com because the stats are glanceable, 97% client retention … people are way too lazy to read these days if they cant read something in sub 3 seconds they wont do it and if you need them to grasp something big you almost have to show them with visuals"). Agreed and deleted: their numbers are self-explanatory at the size they are set; ours ($0, 1,000+) needed their captions to mean anything, and small honest figures at hero scale invite the reader to notice how small they are. The strip's "5.0 on Google · 14 reviews" is the one glanceable figure and already lives in the strip. lib/proof.ts keeps RECEIPTS for the pricing/case pages, where they are context. A before/after was proposed for the slot and dropped — Jake built Desert Wings' original site too, so there is no "before" to show. About now follows the film directly. Recoverable at d5886f6.
- 2026-09-14 · §02 ABOUT · THE SHORT VERSION AND THE REST (Jake: "about section is too long i dont think people will read if its not readable under 3 seconds … a short sweet version and maybe a little paragraph under that says more"). The display line is now seven words — "Built by the person who quoted it." — at --fs-display, answering the kicker; under it a 28-word paragraph at --fs-read, 46ch, --ink-2, with the consequences (no account managers, no handoffs, the price is fixed, the date holds). The ink-resolve stays on the line; its stagger now reads the word count (--n) so the last word still lands at --rp .98. Frame: about-sheet. OPEN: About sits on the old .wrap column (x=75 at 1440) while the film above it settles on the hero's (55) — the 20px step is visible at the seam now that they are adjacent.
- 2026-09-14 · ONE COLUMN (Jake: "yes" to moving the rest of the page onto the hero's). `.wrap` is now the hero's column — `--col-inset: clamp(24px, 3.8vw, 64px)`, capped at `--col-max: 1760px` (the proof row's own cap, so at 2560 the column is 1632 and the widest objects agree); the dock's inset is the same plus the centring past the cap, so the reel's flow box is exactly `.wrap`'s content box. The proof row left its Lando full-bleed (22px off the edge) for the column — the page's one full-bleed moment is the film; the services rows inside the full-bleed card sit on the column too (they were 133 in, the old measure plus the card's inset); the runs panel's own 1760 override is gone because the column is that now. Measured: every content box 55→1385 at 1440.
- 2026-09-14 · THE ORDER: hero → §02 services → About → proof → voices (Jake: "i feel like the video is almost proof"). The reel has just shown the work, so three cards of the same work next would be a repeat; the next question is what you get and what it costs (§02), then who builds it — whose line "built by the person who quoted it" only has its context once §02 has said there is a quote — then the cards as the clickable proof, then the voices. This is the order About was written for ("after §02 and before the proof"); it had drifted to the slot after the hero when its rise was removed. The seam: the film settles into the column and §02's card rises under its bottom edge (29px under at 1.35vh, the lift's shadow on it) and grows to full-bleed — two dark objects on the same two lines. Frame: order-sheet.
- 2026-09-14 · THE STATEMENT AFTER THE FILM — About back in slot two, restaged (Jake: "both itsjay and cosmos have about after the hero or something i wonder why" → "yes do that"). Measured on both sites: what follows their hero is not an about but a POSITIONING sentence — Cosmos "WE ARE — UI/UX design agency that keeps your product growing…" (its About Us is section five), itsjay "MYSELF — …I craft smooth, interactive experiences" — set where attention is highest, and it is what stands between the reel and the work cards so they do not read as a repeat. Ours says the two things the hero does not: "Websites, ads and follow-up for owner-run businesses — built by the person you talk to." (14 words, three lines at --fs-feature — the display rung ran to four), kicker "What this is", the 28-word paragraph under it unchanged. Order: hero → statement → §02 → proof → voices. Supersedes the same-day move of About after §02 (f711396): the fix was the sentence, not the slot.
- 2026-09-14 · §02 · THE OFFER — the statement and the three services in one view (Jake, with danielsnows.framer.website: "a centered approach thing like this and then just having services underneath compared to what we have now" → "go"). Kept from the reference: the shape — a centred caps headline, a grey line under it, 01—02—03, three columns. Not kept: the glass icons (the columns carry the still photographs of the real screens), and the voice (the headline is in the hero's condensed caps so it echoes "DESIGN THAT SELLS"). "WEBSITES, AUTOMATION AND ADS / FOR OWNER-RUN BUSINESSES." — 7vw, two lines, its nouns are the columns in the list's own order; under it "Built by the person you talk to…" at reading size, 52ch. The head is on the ground, OUTSIDE the card's climb wrapper (the climb is measured on the wrapper's top, which must stay the card's top); the space under it is --rise-d + 24 so the climb closes it without covering the paragraph. The dark card keeps its arrival, climb, grow and exit, and now starts ON the column (--s0 = the column fraction, the 84% cap gone) and grows to the gutters. Inside: the path (numerals on ink discs joined by an .18 line — a diagram, not a divider), three columns each a link (well 4:3 · stage · two-line title as authored · label · price), the witness centred under. RETIRED: the split About (its phone was a placeholder), the word-ramp statement with the converging marks, the stacked rows with the spotlight/lens/ride and the phone trays (2026-09-06 — built for stacked rows, no place in three side by side); their CSS is still in room.css, dead, for a cleanup pass. Frames: offer-final (desktop), offer-mobile.
- 2026-09-15 · §01 · GROW, THEN LEAVE (Jake, with danielsnows' hero: "do you think the video effect is too much … has a pretty simple video in hero and it looks nice" → A). Measured theirs: 1380 wide, square corners, below the fold, a 10% parallax and nothing else. Ours keeps the grow — the page's one scroll payoff, the one moment the work is the whole screen — and loses the hold and the shrink-in-place, which were a pin in everything but name: 0.45vh of film choreography between the scroll and the first sentence. Now the reel is the screen at 0.9vh and scrolls off at 1:1, full width, and the offer head starts exactly under its bottom edge (the dock's padding puts the flow box's bottom at page 1.9vh). One number, --p; the --q window, --qs, the pin term and the 24px end radius are gone. d5886f6 has the pinned version. Frames: leave-sheet.
- 2026-09-16 · §01 · THE REEL'S LOAD, MEASURED (Jake: cosmos/itsjay "have videos in hero that are playing as soon as you enter the browser, there's no still image for a couple seconds"). production-stack/references/video.md decoded both live: they reach `playing` at 600–700ms under a frame-exact poster — that is the bar, not zero. Ours, production build, throttled to 4 Mbps/100ms: `playing` at 1.67s before AND after `preload="auto"` + `<link rel=preload as=video fetchpriority=high>` — bandwidth-bound (885 KB), not priority-bound; on a fast line ~180ms. The two edits are kept (no cost, one edge case covered); the poster is regenerated from frame 0 of the shipped MP4 so the poster→video swap cannot pop. scripts/video-timing.mjs measures it (cold context, CDP throttle, listeners via init script). OPEN: a smaller phone source via `<source media>` would cut the phone's number; WebKit may pause an autoplaying video it judges off-screen — the reel's flow box IS below the fold (it is transformed into the hero) — verify on a real iPhone/iPad.
- 2026-09-16 · THE CHROME'S CORNER (Jake, with cosmos.studio: "the new style of buttons and such is these slightly rounded corners, same with nav bar, for professionalism — should EAS do that"). Measured theirs: nav bar 68px at 8px, "Book a Call" 44px at 8px. Yes: one `--radius-chrome: 10px` replaces the pill on the rail, the strip, the hero and nav CTAs, the close CTA and both form input/button pairs — 10 because it is the film chip's radius at rest, so the strip, the button and the picture share a corner. Avatars, dots, badges, chips stay round. Pills read consumer; a small radius on a tall bar reads as an instrument, and it sits with the condensed caps. Frame: chrome-rest. The pill version is ae688a5.
- 2026-09-17 · §01 · THE GROW OVER 0.55vh, NOT 0.9 (Jake: "still not sure what to do about the video effect … maybe it's just a lot of scrolling, not the effect"). The effect is untouched; its scroll cost is 40% less: the film is the whole screen at 0.55vh instead of itsjay's 0.9, and the statement is in reading position by ~1.5vh instead of 1.9 (measured: statement top 165px at 1.5vh). One number, `--grow`, matched to the wrapper's `data-sp-to`; the flow box still ends where the film does (the shortfall between 0.55vh and the letterbox's 511px becomes a negative bottom margin on the dock — padding cannot go negative); the word fades are re-expressed in the new domain at the same pixels of scroll. The dial is those two numbers if it should be faster still. Frames: shortgrow-sheet.
- 2026-09-18 · §03 · THE WORK — supersedes the three-card proof row (0050b34 → 29ae1d2) on Jake's call. The layout is Cosmos's works card, measured (decodes/cosmos-works.md): one centred 3:2 well, 20px radius, the glass label 32px inside the image's top (36px thumb · name · 1px divider · See work), YEAR / NICHE on the page's outer measure at the card's mid-height. The entrance is Cosmos's settle: wrap 1.10→1, image 1.20→1, chased at 0.35/frame (their measured ≈35–40%), measured off the card's own top so the whole settle is visible. The DECK is NOT taken (Jake: "i dont like their effect of the deck coming up"). The step is Apple's highlights (decodes/apple-highlights.md) turned from swipe to scroll: one card per viewport width, no neighbour peeking (Jake), a 0.55vh window per card, dwell .3 / slide .4 / dwell .3, the slide a smoothstep (their ease-in-out, written as f(scroll) — law 11, structure scrubbed), the year/niche fading on the cube of arrival (their caption). The pill has the hover Cosmos's lacks (Jake: "the See work text should go up and See work in a different colour should come underneath it"): the room's own per-letter Roll, the second copy in --work-see-alt. NO backdrop-filter — the 2026-09-06 law stands; the frost is a blurred copy of the project's poster under an ink tint. Phone + reduced motion: the column, 1:1 cards, 8px gaps, no motion (Cosmos's phone; the engine publishing 1 would otherwise park the rail on the last card). Old §03 CSS left in place, dead; cleanup owed. The clips are NOT in the cards: they are 600×600 roster clips and would upscale into an 800px well — stills until per-project cuts exist. Font-sizes taken from the spec's raw numbers (14/12/15px) are sourced from --fs-body-s/--fs-micro/--fs-body instead — byte-identical, but the site's type-scale lock (2026-09-07) forbids a new raw size. (ec708b7)
- 2026-09-18 · §03 · THE WORK v2, THE GALLERY — supersedes the same day's step (ec708b7) on Jake's call: shown v1 in frames he said 'this feels very weird' → 'both' (slide and stop) → 'no i want to find some sort of way to do the horizontal'. What was wrong was not the hold but what filled it: viewport-wide items put the leaving and arriving cards 700px apart (a void), the slide spent 162px of scroll on 1440px (a flick), the dwells were dead time. v2: the card is a FIXED CLIPPED FRAME (Cosmos's 3:2, radius 20) and the sites are a contiguous strip inside it, 20px apart (Apple's gap); the strip is scrubbed CONTINUOUSLY (rx = wp × steps, 0.7vh per card ≈ 1.4:1 at 1440×736, no dwell); when the scroll stops, `lenis/snap` (proximity, 260px, 250ms debounce) lands on the nearest card on Apple's own step curve (ease-in-out, 1s) — the LANDING is theirs, the scrub is ours (law 11: structure scrubbed; the snap is Lenis moving the scroll position, not a tween on the strip — the strip stays f(scroll)). Lenis is reached through `components/anim/lenis-store.ts` (setLenis/onLenis) rather than a context; CSS scroll-snap was not used because Lenis owns the wheel on desktop and the two fight. Apple's pager under the frame (dots, the current one a 48px bar, scrubbed on the same --rx; click scrolls to the card). Year/niche FIXED on the outer measure at the frame's mid-height, three sets overlapped, crossfading on the cube (Apple's caption). Phone + reduced motion: the column, 1:1, 8px, each card with its own static YEAR/NICHE row, no snap (no Lenis on touch). Hold = 1.4vh for three cards, never still.
  TWO BUGS FOUND BUILDING IT, both fixed and re-verified: (1) `--ch` (the frame's height budget) was declared on `.dr-work-frame` itself; `.dr-work-infos` is the frame's SIBLING, not its descendant, and a custom property does not inherit sideways — `.dr-work-infos`'s `top: calc(var(--ch)/2)` read an unset `--ch`, computed to `auto`, and the row landed at the frame's static top instead of its mid-height (measured: setMidY landed exactly on frameRect.top). Fixed by declaring `--ch` on `.dr-work-gallery`, the shared ancestor. (2) the mobile/reduced-motion column put `aspect-ratio: 1` on the `<li>`, but `.dr-work-card` inside it is `position: absolute; inset: 0` (desktop's fixed window) and does not contribute to the li's flow height — `.dr-work-meta` (normal flow, after the card in the DOM) rendered in that same space, and POSITIONED content paints after in-flow content regardless of DOM order, so the opaque card painted directly over the meta row, hiding YEAR/NICHE completely (confirmed via screenshot: no text visible under any mobile card). Fixed by moving the square onto the card itself (`position: relative; width: 100%; aspect-ratio: 1`) so it becomes normal flow and `.dr-work-meta` has room below it (measured: card-to-card gap 64.7px = 8px gap + 56.7px meta height, at 390). Also noted for the record: `window.scrollTo` alone never triggers Lenis's Snap, because `Snap` listens to Lenis's `virtual-scroll` event, which only a real wheel/touch gesture emits — verification had to dispatch a synthetic `WheelEvent` after positioning via `scrollTo` to exercise the actual snap path. (build step following ec708b7)
- 2026-09-18 · nav · THE RAIL WAITS FOR THE STRIP (Jake: "i dont think the nav bar should come in with its animation until after the bar in the hero has disappeared"). The sentinel `.dr-top` is no longer a fixed 19.8svh (the old p .22 mark, written for the 0.9vh grow): the nav sizes it to the strip's bottom edge through the offset chain and re-fits on the hero's resize, so the pill, the lit edge and the action arrive the frame after the hero's white bar has left the top of the screen — after its fade on desktop (p .69), after its box on the phone where it does not fade. Measured: sticks at 344 / 354 / 412px of scroll at 1440×736 / 1440×900 / 390×844, strip opacity 0 on desktop at that frame. 19.8svh stays as the no-JS fallback.
- 2026-09-18 · §03 · THE WORK v4, LEOPARPEIX'S RAIL ON SCROLL — supersedes v2 (49850bb) on Jake's call ('that's the feel I'm going for … activated on scroll instead of drag' → 'go'); the v3 collage (never committed) was a misreading corrected by reading their code (decodes/leoparpeix.md §13). Theirs: uniform 72.57vw × 41.94vw tiles, 20px gap, one infinite rail per project; the hand ×3, a 0.068 follow, a 0.9/frame coast, a 0.055 snap to the nearest tile once the drag force (0.42 attack, 0.065 release, cap 600) dies; the PARALLAXES are on the PICTURE inside its plane — vertical = the block's passage × 8.5% (their uParallaxProgress, on Lenis scroll), horizontal = the tile's screen x ÷ half-viewport × 1.25 × 8.5% (uParallaxX); the SQUASH is depth — vertices pushed back by distance-from-centre × force. Ours: three blocks (one per case), a rail of up to four shots each, tile 0 centred at arrival, the rail riding the block's passage (n−1) tiles at ≈2.4× the page (measured: drift 3195px ÷ 1340px passage = 2.38), chased at 0.1 (the coast); the picture parallaxes ported as translates off a 20% bleed; the depth as CSS perspective 1200px + translateZ(−800px × force × dist), `--force` a NEW engine track (`data-sp-force`, their two constants, saturating at 28px/frame) — the only velocity in the room, and it is scrubbed input, not a tween; the pill on each rail's first tile; their info row under each rail (renamed `.dr-work-title` on the heading, not `.dr-work-name` — the spec's own sketch reused the pill's own class for the h3, which would have let the info row's rule win the cascade for font-size/colour over the pill's). No snap (scroll is the input), no pin (the block scrolls past like §02's rows), no Cosmos settle (the recession already animates arrival). Phone + reduced motion: each rail is the finger's, snap-mandatory, no parallax (R1). Neighbours peek by design here — it is the feel.
  FOUR NUMBERS FOUND WRONG BUILDING IT, all fixed and re-verified live: (1) `--th: min(41.944vw, 70svh)` silently bound at the reference viewport itself — 70svh is 515px at 1440×736, LESS than 41.944vw's 604px, so the tile rendered 515 tall, not the reference's own 604 the pass criteria were measured against; raised to 90svh (662px at 736, clear of 604) — it still exists as a genuine ceiling for a short/ultra-wide window, just no longer binds at the site's own primary viewport. (2) Tailwind's preflight (`img,video{max-width:100%;height:auto}`) clamped `.dr-work-img`'s width to the well's 100% even though our class won the cascade for `width` itself — `max-width` is a DIFFERENT property, uncontested, so it silently halved the intended horizontal parallax (measured 1045px rendered vs. the authored 1254 = 120%); fixed with an explicit `max-width: none` on the rule (the same fix already living at room.css:1789/2696 for the about section's own oversized well). (3) the vertical parallax was authored as a bare `8.5%` on `translate`, which (correctly, per CSS) resolves against the IMG'S OWN enlarged box (724.8px, 120% of the tile) rather than the tile's actual 604 the pass criteria were measured against — off by ~10px at the extremes; rewritten as `calc(var(--pp) * 0.085 * var(--th))`, a px value tied to the tile height directly (`--th` inherits down from the band). Horizontal correctly stays a bare `%` — that one WAS measured off the img's own enlarged width (1254) in the pass criteria, by design (leoparpeix's own uParallaxX every frame, ported literally). (4) the engine's own force-track snap (`Math.abs(f - smooth) < 0.0004`) froze the published value at whatever tiny plateau `f` and `smooth` had both decayed to (e.g. 0.002, forever) rather than 0 — during a release, `f` ITSELF keeps shrinking every frame, so smooth can close the GAP to a still-nonzero, still-moving target long before either reaches true zero; replaced with an absolute floor (`smooth < 0.0005`, matching what `.toFixed(3)` would round to "0.000" anyway), which is the only version that can ever publish an honest rest state. Verified live: real (dispatched, non-trusted but Lenis-smoothed — Lenis does not check `isTrusted`) wheel events cross `--force` 0.5 by ~260ms into a 6×100px/120ms burst and decay under 0.02 by ~1.3s after the last step; raw `scrollBy` jumps (no Lenis smoothing to sustain the mark's velocity) top out around 0.46 on the same burst shape — both are valid per the spec's own 'real wheel events or window.scrollBy' wording, but only the wheel-driven one clears the >0.5 pass criterion, because Lenis is what sustains the input's momentum past the literal 120ms of dispatch.
- 2026-09-18 · §03 · THE WORK v5, ONE ROW, STICKY — supersedes v4's three blocks (6b3d1d1) on Jake's call ('okay so better i just want one row though sticky, that's good though'). One rail of nine shots (cover + two per case) inside a sticky band; the stage is 100svh + 360svh, the rail rides the pinned travel (n−1) tiles ≈ 3.2× the page (their ×3), chased at 0.1; the depth on `--force` and the two picture parallaxes exactly as v4 (the vertical one now spans the pinned travel); the pill on each case's first tile; the info row under the band is three rows overlapped, crossfading to the case under the centre (full while one of its tiles is centred, gone one tile into the neighbour). The hold is back by Jake's ask, and it is never still: the rail, the pictures and the squash all move with every px of scroll. Phone + reduced motion: the finger's rail, three info rows stacked.
  THE PIN is the room's existing no-library trick (`.dr-runs-stage`/`.dr-runs-pin`, §04) — a `position: relative` wrapper 100svh + a declared travel tall, holding a `position: sticky; top: 0` panel; `TRAVEL_SVH = 360` (work-section.tsx) feeds BOTH the `--travel` custom property and `data-sp-to` so they cannot silently disagree.
  A REAL BUG FOUND BUILDING IT, fixed and re-verified live: moving `--th`/`--tw`/`--pitch`/`--drift`/`--rail-x`/`--pp`/`--idx` up from v4's `.dr-work-band` onto the new `.dr-work-stage` (so the rail, tiles AND the new info row could all read them by inheritance) broke the mobile override, which still targeted `.dr-work-band` for `--th` alone. `--tw` etc. are declared ON THE STAGE, so their own `var(--th)` resolves using the STAGE's cascaded `--th` at substitution time — a descendant's override (band) only reaches properties that read `--th` directly at or below that descendant (tile's `height`, which was fine); `--tw` kept resolving the desktop `min(41.944vw, 90svh)` formula against the phone's own viewport, measured 283px instead of the intended 330. Fixed by moving the mobile `--th` override onto `.dr-work-stage` itself, the same element that declares the vars derived from it.
- 2026-09-18 · COPY · THE WORDS CLOSE, THE PICTURES EARN THE READ (Jake: "i need your opinion on content … sense of urgency or compelling people to click … if the beauty of the website is enough … do i need to instill even more trust"). The visuals do one job — the two-second "this person is good" halo — and it is enough to keep the owner reading, never to make them book; the booking needs four plain answers (what, how much, how long, who) and one tiny step. So: NO selling (hype repels the avatar and contradicts the calm the visuals earned), CLOSE instead. Four edits: (1) the hero's door opens the ESTIMATOR ("See your price" → /pricing#estimate) — for a cold ad visitor a call is a big ask on our schedule, a price in sixty seconds is about them; "Book the call" stays the nav's persistent action and the close; (2) the offer's paragraph carries the three promises as one triplet — a fixed quote in two days, a price that never moves, a date that holds — the decision line, 27 words; (3) the rail's info row gets LIVE ↗ (the client's real site, new tab) beside "See the case" — a site they can open is proof nothing else buys; (4) `NEXT_START` in lib/proof.ts — the ONE honest urgency, "One build at a time — next start <date>" in the close, shown only while Jake keeps the date true (null hides it); countdowns and "spots left" are banned by the no-fake-proof rule. Left as is, flagged: the de-named placeholder quotes in Voices are worse than none — two real names or cut the section; the About photo and the Google rating remain the trust gaps no copy can cover.
- 2026-09-18 · §01 · THE TITLE'S RISE IS LEOPARPEIX'S, AND IT HAS A SECOND READING (Jake: "the french interactive designer text can you look at the effects for it i really like that entrance" → "go ahead and build it, on hover for the swap"). Read from their code (decodes/leoparpeix.md §14): each line one line below its mask AND 5% sideways, alternating by line, rising 1.0s on cubic-bezier(.4, 0, 0, 1), 115ms apart. Ours: the title from +5%/103% at 300ms, the strip as the SECOND line from −5%/40% with its fade at 415ms (unmasked — a mask would cut its shadow), both on that curve. THE SWAP on hover: "Design that sells" leaves downward on the opposite diagonal (.9s) while "Sites that book" rises in behind it through the same mask from +.38s (1.2s) — their 1.5s/2.0s-at-.63 scaled to a hover; both copies of the line (ink and the film's white clip) carry both readings. ⚠ `--ease-reveal` is the ONE declared exception to the two-curve law, hero only; nothing outside §01 may use it. Fix on the way: the line had no `position`, so the absolutely placed second reading was outside the mask's containing block and sat in plain view under the first (screencast) — `.dr-line:not(.dr-line--film) { position: relative }`. Reduced motion: no transition. NOT added to the statement after the hero, on purpose: a hero entrance repeated one section later reads as a template, and the room's one triggered vocabulary (the wipe, line 21) stands there.
- 2026-09-18 · §01 · THE SECOND READING IS OFF (Jake, an hour after asking for it: "take away the hover effect off the hero its a cool idea but im not sure" · "the user hovering on the title isnt something that would happen naturally"). Agreed, and the second line is the law worth keeping: a hover on a headline is not a gesture a visitor makes — nobody hovers non-interactive text, so an effect that lives there is an effect nobody sees. Hover work belongs on things that already invite the pointer (pills, cards, links). The diagonal rise on `--ease-reveal` stays; the swap is recoverable at 5eef8b3.
- 2026-09-19 · §01 · THE INTRO AND THE SEAT ARE NOT TAKEN (Jake, watching it build on the dev server: "i feel like i might like the last version better"). The velour-style opening — an intro card over the film's buffering, a hard cut to the film full screen, a hold, the shrink into the seat — was specced and half-built, then stopped before commit; it lives in `git stash` ("§01 intro + seat") for the record. The hero stays as it was: the film in its letterbox from the first frame, the 0.55vh scroll-grow, the diagonal rise. Worth keeping from the exercise: the drama-from-time-not-scroll reading of velour (decodes/velour.md) and the honest-loader idea (a hairline that fills with the film's REAL buffer, never the word loading) — if an opening ever comes back, that is its shape.
- 2026-09-19 · §02 · THE CURTAIN (Jake: "the what this is section isnt dramatic enough" → "go on the curtain"). The statement sits PINNED under the film: the stage begins at doc 0.55vh (the film full screen), the pin holds a screen and 80svh more (`--say-hold: 180svh` — see below, not the 30 first assumed); as the film leaves at scroll's rate its bottom edge uncovers the words — kicker, the two lines settling 1.12→1 on the same progress, the paragraph — and then the services card, pulled up a screen (less `--rise-d`, see below), climbs over them. Uncover → hold → cover: three surfaces in a chain, the words never travel. NOT the 2026-09-06 curtain that was reverted — that one put a NEW light-room plate mid-page; this uncovers the statement in the room's own ground and ink, already sitting there, no new surface. The wipe comes off both headings entirely, not just neutralised (services-section.tsx has the why: a wipe under a curtain fires unseen, AND — found live — the engine's own splitLines()/undo() cycle did not survive being inside the new sticky pin, on either width: it split correctly on mount then silently reverted to plain text ~300ms later, confirmed by hand-running the identical split logic on the same node, which persisted fine; something about the sticky-pin position, not the CSS, breaks its lifecycle). Seam devices are now: film→statement CURTAIN UNCOVER · statement→§02 climb-over-pin · §02→§03 fade+whitespace · §03→§04 whitespace-only seam + repeated opener · §04→§05 the page's one ground flip · §05→§06 bookend dome mask (law 1 holds, six distinct — hero→§02's old "sticky-cover climb" is retired, replaced by the two above). TWO REAL NUMBERS FOUND BY MEASURING, not guessed: (1) the spec's first `--say-hold: 130svh` landed the card fully over the words at 1.55vh — before the hold even started — because `.dr-svc-in`'s own pre-arrival RISE (`--rise-d`, ≈.26vh at 736) makes the card's VISUAL top outrun its wrapper's plain layout top by that much; raised to 180svh so the card is still below the fold through the hold and only arrives once it is over. (2) `.dr-climb-wrap`'s margin, following the spec exactly at `-100svh`, put the card's visual top at y 0 ~150px before the pin actually released — the wrapper's plain-layout top and the pin's release always coincide exactly (both reduce to the same `stageDocTop + say-hold` sum), but the card's OWN rise made its paint position outrun that by a constant ≈.26vh; margin corrected to `calc(-100svh + clamp(110px, 26svh, 300px))` (`--rise-d`'s own formula, duplicated — a child's custom property cannot be read by its parent) brings the two within 39px. THIRD BUG, unrelated to timing: `.dr-slot` (the hero's transparent placeholder, aria-hidden, z 6 via `.dr-stage`) and `.dr-dock` (z 5, the reel's own static flow box, far bigger on screen than the transformed reel once the curtain put the pin in the same first screen) both lacked `pointer-events: none`, so `elementFromPoint` over the pin resolved to one of them instead of the video or the words; fixed on both, with `.dr-hero-reel` re-opted into hit-testing since it is what the checks need to resolve to while the film covers the pin. Measured (1440×736): --cur 0/.50/1.00 at scroll .55/1.05/1.55vh; pin.top 0 throughout the hold; h2 centre 350px (±18 of the true 368 centre) from .55vh on; scale 1.12→1.06→1.00; card visually covers by ~1.9vh, its top reaching y 0 at scroll 2.40vh against the pin's release at 2.35vh (39px, within the ±40px asked); total scroll from film-full-screen to card-top-0 ≈1.85vh (the spec's own target was ≈1.6 — the --rise-d correction above cost the difference, and was kept because the ±40px release alignment is the harder, numeric ask). Page height at 1440×736: 10629px → 11012px (+383, mostly the card's own new content, below). Phone + reduced motion: the statement in flow as before, no wipe (the engine bug above applies to both), no scale.
- 2026-09-19 · §02 · THE CARD FILLS OUT (Jake: "the services one thing is too short" → "do both"): three deliverable lines under each price from lib/services.ts (see-what-you-buy; what's IN it, not just what it is) — `t-label`'s register, sentence case, `var(--ink-3)`/`var(--ink-4)` dot, a hairline `var(--edge)` above — and the one-line quote becomes a pull quote at display size with its attribution (still de-named — a real name is owed), avatar (40px) centred above it. The spec's named fallback size for the quote, a raw `clamp(1.35rem, 2.1vw, 1.9rem)` (--fs-feature ran the 14-word quote to 7 lines at a 30ch measure), would have broken the 2026-09-07 type-scale lock (a new raw size) — `var(--fs-section)` used instead, measured at the same 2 lines. `max-width: 30ch` had to move from `.dr-vouch-body` onto the blockquote itself: `ch` resolves against an element's OWN font-size, and the body's (unchanged, ~15px) made 30ch a constant 340px regardless of how small the blockquote's own font got. Card 721px → 990px at 1440 (+269, the three lists plus the taller quote).
- 2026-09-19 · §02 · THE CURTAIN, TRIMMED (Jake, on the first cut: "if we are having the curtain it shouldnt be sticky and services shouldnt rise like that and also you can see the text of second section under hero video"). Three changes: (1) the pin holds ONLY for the uncover — the film's own exit, one screen — plus a 30svh beat with the words alone, then releases and the words scroll like anything else (`--say-hold` 180 → 130svh); the sticky is the mechanism of the curtain, nothing more; (2) THE COVER is gone — the card is not pulled up over the words, it follows the stage in flow and its top is at the fold the moment the pin releases (measured 735 / 900 / 1080 at the three heights); its own small climb from the 14th stands; (3) the words are TRANSPARENT until the film is the screen — a `--pre` track on the pin (its top from the fold to the top, the frame the film goes full screen) gates the head's opacity over the last 4% — because the pin is pulled up under the letterbox and on a 900-tall screen the statement's lower half sat below the letterbox's bottom edge on the first screen (h2 823–1026 against the film's 888). Measured at 1440×736 / 1440×900 / 1920×1080: opacity 0 at scroll 0 and 0.3vh, 1 at 0.55vh with the film covering; half uncovered at 1.05vh (scale 1.06); words alone at 1.55vh (scale 1); release at 1.85vh with the card's top at the fold; no cover at 2.1vh.
- 2026-09-19 · §03 · THE RAIL STEPS (Jake: "can we go back to the work rail. im thinking i want it to be a little more of a controlled motion like it kinda makes you go one at a time"). The continuous scrub (v5) becomes a stepped map: each of the 8 windows (52.5svh, travel 420svh) is dwell .2 / slide .6 / dwell .2, the slide a smoothstep (Apple's ease-in-out, decodes/apple-highlights.md) — the rail holds a tile, moves one pitch, holds the next; the depth squash follows the SLIDE (`--sl`), so tiles recede only while the rail moves; the pictures' vertical parallax stays continuous so a hold is never dead; `lenis/snap` (v2's, 49850bb) lands a stop on the nearest plateau on Apple's curve — the snap moves the scroll, the rail stays f(scroll). The v1 flick this resembles on paper was a 700px void between viewport-wide items and a 162px slide; here the tiles are contiguous, the slide is 232px and eased, and the neighbours are in the frame the whole way. A REAL BUG FOUND WHILE VERIFYING: `lenis/snap` listens to Lenis's own `virtual-scroll` event, which only its `VirtualScroll` sub-object's wheel/touch listeners emit — a bare `window.scrollTo` (or `lenis.scrollTo`) never fires it, so testing the snap needs a real dispatched `WheelEvent` burst, exactly like the force track's own note already warns for a different reason. Measured at 1440×736: at t 1.0/1.1/1.2 (`t = bp × 8`) `--rx` = 1.000 and tile 1 stays centred at 719.99px through the whole dwell; at t 1.5, `--rx` = 1.4993; at t 1.8/2.0, `--rx` = 2.000. The slide moves one pitch (1065.02px measured) over exactly 232px of scroll (3867→4099) — ratio 4.59, on the ≈4.6 target. `--sl` reads 0 at t 1.0/1.2 and 0.999999 at t 1.5; with `--force` forced to 1 inline the edge tile is unsquashed (1045.02px) at t 1.2 and squashed to 627.01px at t 1.5 (1045.02px at rest). A real wheel burst from rest at plateau 1 toward a mid-slide target lands, 1800ms later, on plateau 3 (scrollY 4562.5 of 4562.71, `--rx` exactly 3); 1000px past the last plateau a further small wheel nudge moves exactly that far with no pull back. The infos crossfade lands squarely on the slide: `[1,0,0]` at t 2.2 (slide not yet started), `[.5,.5,0]` at t 2.5 (mid-slide), `[0,1,0]` at t 2.8 (slide just finished). A `scrollBy(0,300)`-equivalent nudge from a plateau lands `--rx` back on an integer by ~900ms and scrollY fully settled by ~1.3s, inside the 1.5s budget. Phone/reduced-motion unaffected (`window.lenis` stays unset below 900px — the effect's own media-query guard). `.dr-work`'s height: 4055px ≈ 5.51× viewport height at 736. Page's own pre-existing ~25px horizontal overflow (present on plain v5 too — confirmed by stashing this diff and re-measuring before re-applying it; cause not this step's, not chased down here) is untouched by this step.
- 2026-09-19 · §03 · THE RAIL ANSWERS THE FIRST NOTCH (Jake: "theres a lot of delay when i start to swipe vs when it goes"). Three causes, three dials: the dwell (.2 of a window = 77px of scroll in which nothing moved) → .03 (12px — the snap is what puts the rail on a plateau, the dwell only has to be one); the ramp (smoothstep starts at slope 0) → half ease-out, half smoothstep (f = .5·(2s − s²) + .5·(3s² − 2s³): slope 1 at the start, 0 at the landing — the arrival stays Apple's, the response is what changed); the chase (.1) → .2. Measured from a plateau with a real six-notch wheel swipe: the rail first moves at 168ms after the first notch (of which ~85ms is Lenis starting the page itself), against ~250ms+ before and ~400ms with the .2 dwell; it still lands on the next plateau to the pixel.
- 2026-09-19 · THE ROOM · DARK IS THE DEFAULT AGAIN — A TRIAL (Jake, with x2ycreative.com: "i kina want to try dark … make dark the default i just want to try it"). x2y measured: ground #0b0b0b (not #000), white 500-weight condensed display at 172px, body #878787, one yellow accent, NO card surfaces — full-bleed work on the void with 352px spacers; drama = scale + emptiness; a music studio, whose audience is the dark-room audience. Ours: the one boolean inverted (shell.tsx `useState(false)`, `?light` restores the light room for comparison — the light era's rules stand under `.dr-light`, untouched, and the 2026-07-17 Apple-cool palette is still the light room's), the void softened to x2y's #0b0b0b in the three places it is set (the token, the flip mix, the first-paint body), and the strip made a DARK BAR with a hairline in the dark room — as a white bar it put the white door on white; now the door is the one white object again (the room's own law). The services card stays the white PUNCH the dark room was designed with. ⚠ This reverses the light default of 2026-09-13 on a "want to try", not on a measurement; the buyer question (a flight-school owner at noon reads black as agency-for-agencies?) is still open and is what should decide it. Both rooms run from one URL flag, so the comparison costs nothing.
- 2026-09-19 · §02 · THE SERVICES ON THE VOID, dark room only (Jake: "i dont like services" → "do the services card on the void"). x2ycreative has no card surfaces — the work sits on the ground with space and hairlines. In the dark room the services lose their card: no light punch, no surface, no grow (the ::before is `display: none` — a transparent surface scaling would be a composited layer for nothing); the steps line, the three columns (title/what/price/the three deliverable lines) and the pull quote sit on #0b0b0b in the room's own ink, the phone trays go to the shots' near-black, and a hairline above and below the column keeps the beat's shape. The climb and the exit fade stay (motion, not surface). The light room keeps the dark card it was designed with (`?light`, verified: ::before #121214, block). Measured in the dark room: `.dr-svc-in` background transparent, ::before none, title #fff, price #d4d4d8, lines #949498, wells #1c1a20.
- 2026-09-19 · THE MODEL · hugeinc.com (Jake: "im really feeling this site" → "yea this site will be our model"). Supersedes the reference cross of 2026-07-01 (itsjay cards × eloqwnt professionalism) and the per-section references since (Cosmos, Apple, leoparpeix, x2y) AS THE MODEL — those decodes stand as measured parts, but the site's grammar is now Huge's: one type family at three sizes (285 / 146 / 43 at 1440), one accent (a magenta) used sparingly, a WHITE page with a BLACK hero, a pinned "Our work." with the cases sliding horizontally under it, a Solutions ledger of 146px lines with hairlines, and a hero that speaks to the day (a four-key day table) and to the return visit (four escalating lines with their own door labels). The full decode is being measured (decodes/hugeinc.md). ⚠ HOW IT IS APPLIED (feedback_hero_that_landed, 2026-09-13: "whole concepts lifted from a reference lost every time"): one section at a time, each verified in real Chrome and shown, never the whole site swapped in one build. ⚠ OPEN, Jake's call: Huge is white with a black hero — the dark default of this afternoon is a trial; the model says light page + dark hero, which is neither of the two rooms as they stand.
- 2026-09-19 · §02 · THE LEDGER — supersedes the three columns (2026-09-14, danielsnows) on Jake's call ("still not really feeling services section"). Diagnosis: nine small things in a grid between two big cinematic beats — a pricing-table archetype, and the void made it plainer. x2ycreative measured: six services one per row, 760px each, a 108px title left, one grey paragraph, one 471px square video right, hairlines, no cards. Ours: three rows ≥ 62svh, hairlines, 7:5 text:shot, the title at the statement's rung, price under it, the three deliverables as one sentence, "See the service →", the shot as one big 4:3 well; each row's shot settles 1.2→1 (Cosmos, the rail's own) and its text rises 24px as the row arrives; ALTERNATING sides shipped (row 2's well on the left) — three rows is short enough that the zigzag reads as rhythm rather than as inconsistency in the built frames (ledger-row1.jpg, ledger-row2-arrive.jpg). A REAL BUG FOUND BUILDING IT: a bare `order: -1` on the well for even rows does not just mirror the side on a two-fr-track grid — it moves the well into whichever cell auto-placement fills first, so the well landed in the WIDE (7fr) track and the 7:5 ratio inverted (measured: well ballooned to 734×550, row to 653px tall, vs the intended 524×393 / 496px on odd rows). Fixed by swapping `grid-template-columns` to `5fr 7fr` alongside the `order` flip on even rows, scoped inside `@media (min-width: 901px) and (prefers-reduced-motion: no-preference)` — unscoped, its 3-class specificity beat the single-class mobile/reduced-motion collapse rule regardless of the media condition, and an even row measurably kept a 2-column grid (120px/168px) at 390 until this was scoped. The steps line is gone (the row numbers carry it, `.dr-ledger-num`). One idea per viewport — the law the columns broke. `.dr-offer-steps`/`.dr-offer-grid`/`.dr-offer-col`/`.dr-offer-well`/`.dr-offer-stage`/`.dr-offer-num`/`.dr-offer-title`/`.dr-offer-what`/`.dr-offer-price`/`.dr-offer-inside` are DELETED from room.css, not left dead; `.dr-offer-vouch`/`.dr-vouch` and the dark-room void tokens on `.dr-svc` are UNCHANGED — the void rule's own `border-block` on `.dr-svc-in > .wrap` was removed, since the rows now carry the hairlines themselves and it would otherwise double the top/bottom lines. Cost: `.dr-svc` grew 1.58vh → 2.69vh at 1440×736 (1161px → 1981px, +820px), the page +820px (11778 → 12598) and the scroll to `.dr-work-stage` moved by the same 820px — nothing else shifted disproportionately. Both rooms verified (dark: `.dr-svc-in` background transparent, ::before none, title #fff, body #949498, hairlines var(--edge); light `?light`: ::before #121214 block, ledger inside it in the same ink).
- 2026-09-19 · THE ROOM · WHITE PAGE, BLACK HERO (the model, hugeinc.com — Jake: "yea lets do white page with black hero"). Supersedes the dark default of the same afternoon (a trial) and the light default of 2026-09-13 as the SHAPE: the light room is the page again (shell.tsx `useState(true)`, `?dark` is the comparison), and the hero — from the page's top edge to the film's own bottom — is a DARK ZONE regardless of which room is active: `.dr-hero-wrap.dr-zone-dark` re-declares the dark room's own ink/surface tokens and paints `#0b0b0b` on a `::before` from the wrap's top down to EXACTLY the film's frozen post-grow bottom edge — not the wrap's own border box, which (measured, 1440×736: 1247.75px) runs 106.95px past that edge into the words' own ground, because `.dr-hero-wrap`'s `overflow-x: clip` computes `overflow-y` to `auto` too, which blocks `.dr-dock`'s negative bottom margin from collapsing through. The true edge is a geometric invariant instead — one viewport (`.dr-stage`'s own 100svh) plus `--grow` — duplicated as a literal on the wrap rather than read cross-tree (custom properties only inherit downward, and `.dr-dock` is a descendant, not an ancestor): `height: calc(100svh + 55vh)`, RE-MEASURE both together if `--grow` ever changes. Mobile (no grow, no pin) is simpler: the wrap's own rendered height already ends at the film's true bottom there, so the zone is just `height: 100%`. The curtain now lifts a black film off a white statement — Huge's own seam — unchanged mechanically; the strip/faces go dark inside the zone (mirroring the existing `:not(.dr-light)` rule, re-scoped, since `.dr-light` is now present and would otherwise win). The rail (`components/room/nav.tsx`) flips from dark chrome to light as the film's bottom passes its own hem: a second sentinel, `.dr-ground-end`, at the same document position, read against `.dr-rail`'s own `getBoundingClientRect().bottom` on a rAF-batched scroll/resize check (not a single IntersectionObserver rootMargin — a sentinel this thin gives `isIntersecting` the same false reading whether it is still off the bottom of the screen or has already passed above the hem). The rail re-declares its own copy of both colour sets, because it is a RoomShell sibling of the page's content, not a descendant of the zone, and cannot inherit its tokens. A REAL BUG FOUND VERIFYING: the ground-flip logic did not check which room was active, so `?dark` — a page with no light ground to ever flip into — still flipped the rail to light chrome once scrolled past the sentinel; fixed by re-checking `.dr-root`'s own `dr-light` class on every call (not just at mount, since shell.tsx's own `?dark` effect and this one run in undefined order across components). Measured at 1440×736: hero ground `#0b0b0b` ±0 at (720,30); title `#fff`; strip `#121214` with the white door; ground's frozen bottom (scrollY + reel.bottom) settles on 1140.5px for every scroll ≥ 405px (`--grow`), matching `100svh + 55vh` = 1140.8 within a px; the curtain's own numbers (opacity 0/0/1 at 0/0.3/0.55vh, scale 1.12→1.06→1, ink `#0a0a0b` once uncovered) are unchanged; the rail's flip lands within ~5px of `1140.8 − hem` (measured hem 79.66px → flip ≈1061–1065, expected 1061.14) at 1440, and within ~5px of the same identity at 390 (hem 76.25, flip ≈521–530, expected 526.15); settled dark chrome `rgba(11,11,13,.92)`/`#949498` links/`#171719` CTA, light chrome `rgba(255,255,255,.86)`/`#5c5c63` links/`#0a0a0b` CTA, `.dr-links a`/`.dr-navcta` cross at `var(--t-micro)` (300ms); frame probe across the seam (0.6→1.6vh, 8 steps) max 8.6ms; a full settled `scrollBy` sweep 0/150 frames over 34ms. Two reload-screencast runs, one clean: the FIRST showed the fresh Playwright browser's very first request hitting a stale pre-edit Next.js dev build (a compile-cache artifact, not a code defect — confirmed by the second run, taken after the dev server had already compiled everything from live testing, showing the correct dark-hero frame from t=15ms with no flash to the inverse). THE ATMOS: `.dr-atmos`/`.dr-vignette` are already invisible throughout the light room (their own `opacity: calc(1 - var(--flip,0))` reads `--flip: 1` from `.dr-root.dr-light`, unrelated to the zone); `.dr-wall`/`.dr-figure` do not ride `--flip` at all and stay visible everywhere else, but the zone's ::before is opaque and — being a `z-index: auto` sibling declared after `.dr-wall` in paint order — sits in front of it wherever the zone covers it, hiding it there with no code change needed; verified live, no bleed-through at 1440 or 2560. ⚠ PRE-EXISTING, NOT THIS STEP'S: under `prefers-reduced-motion: reduce` at desktop widths, the reel's transform math already produces a broken, off-screen rect (confirmed on the unmodified code via `git stash`) — the "engine publishes `--hero-p` as 1" comment does not hold cleanly in practice; the zone's own reduced-motion fallback (`height: 100%`, shared with mobile's media query) does not make this any worse, but the reel itself is not usable there regardless.
- 2026-09-19 · THE LAW OF THE MODEL · BIG, FEW, VISUAL-LED (Jake: "my favorite thing about the site is the amount of text and sizing. its so visual led. the text is big and there is not much of it, its simple and to the point, powerful yet concise"). Measured on hugeinc.com at 1440 (the whole page scrolled so every reveal has fired): 221 words on a 19,902px page — 8.2 words per screen — and 7 of them under 24px (0 under 16 that matter). Every line is display size: 285 / 146 / 57 / 43. Ours the same day: 473 words on 12,517px — 27.8 per screen, 3.4× theirs — and 354 of them under 24px (the ledger's deliverable lines, the runs, the objections, the info rows). THE LAW, from here: (1) nothing on the homepage is set under 24px except the nav, labels of ≤3 words, and legal; (2) a section's copy is one line at display size and at most one sentence under it; (3) the budget is ≤10 words per screen of scroll, ≤200 on the page; (4) what a small paragraph used to say, a picture says or a case page says — the homepage points, it does not explain. Applied one section at a time (the standing rule), the runs and the objections first (92 and 33 small words), then the ledger's lines.
- 2026-09-19 · THE CONVERGENCE (Jake: "i want to make my site as close as possible to theirs [hugeinc.com], except i definitely want a video in the hero … huge and clay.global have very similar layouts with huge being slightly more fun … it does show what the trend of big agencies is" → "sentence case, propose three colors, yes take them off"). Clay measured: white page, one 74px line, THE VIDEO FULL-WIDTH RIGHT UNDER THE HEADLINE (1290×726 at y 737) — the film's placement is the trend's, not a deviation; 1,702 words of which 1,498 are captions under 24px — Huge reads as more fun because it says less, bigger. THE LINEUP from here, Huge's with our film: (1) black hero — one short line at the 285 rung + the day line + the film (our grow); (2) the statement at the 146 rung with the rotating word, the paragraph under it cut; (3) no logo band; (4) OUR WORK in Huge's form — "Our work." pinned, wide cards 1360×464 at 40px pitch, the case name + one line ON the card at 57px, 1:1 linear scrub, a one-screen hold at the end, hover = the image 1.05 — the step, the squash and the picture parallax retire, the pill stays; (5) "Built by the person you talk to." at 146 — the about beat, the photo when it exists; (6) the ledger as three off-white cards — 01 Websites. / 02 Automation. / 03 Google Ads. at 146, one line + the price at 43, the shot on the right — the deliverable sentences cut; (7) the voices as three cards at 43 (the ideas slot, until a blog exists); (8) the close at the 249 rung: one word, one line, the door. THE PROCESS AND THE OBJECTIONS LEAVE THE HOMEPAGE (they live on /services and /pricing) — 125 of the 354 small words. TYPE: sentence case, one weight — the condensed caps retire from display (Jake's call; the identity risk was named). Target ≤200 words. Executed one section per build, each shown, top to bottom.
- 2026-09-19 · THE ACCENT · ELECTRIC CYAN #1ee5ff, USED LANDO'S WAY (Jake, after 25 candidates on both grounds: "electric cyan is my favorite but it doesnt really work on white" → "i think we actually can make it work … the lando norris site does a really good job at dealing with a super vibrant color … lando does a good job at creating situations where its on a dark background to use accent color … b could occasionally work on white"). Lando's rule, measured (signature-frontend/references/lando-section-grammar.md): the saturated lime lives on the dark-green ground and on the one pill; the statement's accented words are the OFF lime (#b2c73a, 7.6:1) — TWO limes, and the page CREATES the dark situations the bright one needs. Ours: `--accent: #1ee5ff` (12.8:1 on #0b0b0b) as text and fill wherever the ground is dark — the hero (the day line's key word, the door's fill with ink type, the numerals), any dark card (the ledger's cards may go dark for exactly this reason), the close if it goes black; `--accent-off: #0aa9c2`-class twin for the rare word on white (≥4.5:1 — pick the exact value when it is first used); on white otherwise the accent is a SURFACE — the cyan door with ink type everywhere (the one bright object on every screen), and B, the underline drawing in under the rotating word, as the occasional white-ground use. Never cyan letters on white (1.5:1). The rotating word: cyan text on black, ink + cyan underline on white. This sets the lineup's grounds: the page will hold MORE dark sections than Huge's so the colour has rooms to live in — the hero, the work rail (black, as Huge's is), the ledger cards, the close.
- 2026-09-19 · THE TYPE SYSTEM OF THE MODEL (Jake: "sentence case" · "alright lets get to work"). Huge measured (decodes/hugeinc.md §2): ONE family, ONE weight (600), sentence case, five rungs at 1440 — 284.8 / 145.65 / 57.46 / 43.25 / 19.1 — tracking −0.07em on the hero, −0.05em on the rest, tight leading (cap-height set). Ours from here, on Archivo wdth 100 wght 600 (the condensed caps retire from display): `--fs-h1: min(28.5vw, 17.8rem)` [285] · `--fs-h2: min(10.2vw, 9.1rem)` [146] · `--fs-h3: min(4vw, 3.6rem)` [57] · `--fs-h4: min(3vw, 2.7rem)` [43] · `--fs-p: 19px` [body — the only size under 24 on the homepage besides labels]. Tracking −0.07em / −0.05em / −0.04em / −0.03em; line-height .9 for h1–h2, 1.0 for h3–h4, 1.35 for p. Every homepage heading takes a rung; the old --fs-display/feature/section tokens stay for the interior pages until they are converged. The order of the pass: cuts (process + objections off the homepage) → hero + statement on the new rungs → the rail in Huge's form → the ledger as three cards → the about line → the voices → the close.
- 2026-09-19 · §01 · THE DAY LINE AND THE RETURN (Huge's, decodes/hugeinc.md §3: a four-key day table under "Hello.", a four-line return table with escalating doors). Ours in the honest-capacity register, never quippy — Monday: "Monday. Quotes go out by Wednesday." · Tue–Thu: "Midweek. A call today, a fixed quote in two days." · Friday: "Friday. Book now, start the plan over the weekend." · weekend: "Weekend. Book the call now, we reply Monday morning." · 2nd visit: "Back again. The price takes sixty seconds." (See your price) · 3rd: "Third visit. The call is twenty minutes." (Book the call) · 4th+: "You keep coming back. Let's talk." (Book the call). Phoenix time, the visitor's clock (client-only; the server renders nothing and the height is reserved so the strip never jumps); localStorage `eas:visits`. Under the h1 on the column's left edge, 15px, ink-3. lib/greeting.ts.
- 2026-09-19 · §02 · THE ROTATING WORD (Huge's, §4: the accent word rotates on a timer, 1250ms hold, 700ms cross). Ours: the statement's last phrase cycles six real audiences — owner-run businesses · flight schools · coaches · clinics · nonprofits · contractors — the ad-audience personalisation made visible; CSS keyframes staggered by index (11.7s lap, no hydration needed; reduced motion holds the first word), the server renders the true statement. THE COLOUR, per the accent decision: on white the word is INK with the cyan drawn under it (its own ::after, scaling in over the first 600ms of the hold, leaving with the word); on a dark ground the word is cyan. The builder that made it stopped on the conflict between its spec (an ink placeholder) and the newer accent decision — the decision won.
- 2026-09-19 · §01 · THE SNOWS HERO — TRIAL B on branch `hero/snows` (Jake: "i wonder if we can find a layout like this make this work somehow" → "build B the snows one"). danielsnows measured: the one word at the screen's full width (365px, 1420 of 1440), three small meta columns under it on hairlines, the video full width with its top just above the fold. Ours is the hero we had with two changes: "Welcome" sized by measure to the column's width (measured 350.8px at 1440, 90.1px at 390), and the strip replaced by the three-column meta row (the day line split at its period · the rating · the three services) — the door leaves the hero for the nav. The film, the grow, the curtain: untouched (re-verified live against the new hero above them — all curtain/grow numbers hold unchanged). Against A (`hero/welcome`): A moves the film to the corner and grows it from there; B keeps the film where it is and makes the word the screen.
- 2026-09-19 · §01 · THE WORD'S SIZE — MEASURED, NOT THE SPEC'S OWN NUMBERS, three times over (page.tsx has the full note). (1) `getComputedStyle(h1).fontWeight` reads the CSS `font-weight` property, not `font-variation-settings`'s "wght" axis — `font-weight: 600` is now declared alongside the axis on `.dr-h1.dr-welcome` so the canvas measure and the real paint agree; without it the measure silently used weight 400. (2) Filling the 1330px column at 1440 measures ≈350px, not the spec's "≈300" — corroborated by Snows' own "SNOWS" at 365px — so the spec's "cap at 17.8rem" (284.8px, the type system's own --fs-h1 ceiling) is DROPPED: honouring it would leave the ink ~250px short of the column's right edge, against the same spec's own pass criterion (ink right edge 1385 ±3). (3) `actualBoundingBoxLeft/Right` (true ink pixels) measured "Welcome"'s trailing 'e' overshoot as ~2px wider than its ADVANCE box, compounding to a 6-7px shortfall at hero scale — switched to `m.width` (advance), which matches a live DOM Range's own `getBoundingClientRect()` (how this was verified) to within 0.01px. The 96px FLOOR is lowered to 64px for the same reason in reverse: the unfloored formula already lands at ≈90px at 390 (matching the spec's own expectation), and 96 would have pushed the ink 22px past the phone column's right edge into the gutter. The bearing margin (`.dr-welcome .dr-line`'s `margin-left`) is 0, not the spec's -0.03em guess: "Welcome"'s W carries almost no left bearing at this weight/width, and 0 landed the ink within 0.3px of the column's left edge live.
- 2026-09-19 · hero/snows · THE ROW UNDER THE WORD GOES; THE DAY LINE AT 43 STAYS (Jake: "i dont like the area under the welcome in b"). The three-column meta row — 12px labels on hairlines between two giant things — was the smallest text on the page in exactly the place the model has its second-loudest line. Replaced by Huge's own second voice: the day line at the 43 rung, white, one line, on the column's left under the W (measured: 43.2px, x 55, 19–23px above the film at 736/900/1280; two lines at 390). The rating and the three services leave the first screen; the nav carries the action. The `.dr-meta` name stays on the line for the sentinel and the fades.
- 2026-09-19 · hero/snows · THE FILM SITS LOWER, THE GROW IS AS LONG AS THE SLOT IS HIGH, THE E IS UNCLIPPED (Jake: "i dont think the video reel has to come up so high, in the reference it doesnt, also the e is clipped on the right"). danielsnows shows 112px of video above the fold; ours showed 256. Now `--slot-show: 150px` — the slot's top lands at the fold less 150 (measure() hands the stylesheet the day line's bottom, `--day-b`, and .dr-slot's margin grows to it; the clamp is the floor on a short screen). THE GROW'S DISTANCE IS THE SLOT'S TOP: the film's top edge leaves at scroll's rate, so it is the screen the instant the page has scrolled --t0 — measure() writes `--grow` on the wrapper (586px at 736, 751 at 900) and the engine takes the same number in px (`data-sp-to-px`, read each frame, wins over the vh fraction); the dock no longer declares its own --grow (it shadowed the wrapper's and the film stopped 181px short); the dark ground's end (`100svh + --grow`) and --say-pull follow the same number, so nothing can drift. Cost: the film is the screen at 0.8vh instead of 0.55 — 181px more scroll at 736 — the price of the lower slot. THE E: the rise's mask clipped the last e's overshoot past its advance — the line gets .06em of room on the right (the old title's S had the same) and the measure aims .8% inside the column. Measured: reel [0,0,1440,736] at 586 / [0,0,1440,900] at 751; frozen bottom = ground end = 1322 / 1651; the curtain and the rail's flip unchanged in kind.
- 2026-09-19 · hero/snows · THE DOOR RETURNS TO THE HERO (Jake, on the band the lower film opened: "now how can we use that space"). The one action, back where the first screen decides: the cyan pill under the day line on the column's left — "See your price" (the estimator), or "Book the call" from the third visit (lib/greeting.ts) — ink type on the accent, 52px, the last thing to arrive in the entrance (680ms). The slot's height above the fold is measured off the door now (the last thing above it), so the film keeps its 150px. The rail's sentinel ends at the door. Phone: full width.
- 2026-09-20 · hero/snows · THE LIVING MARK (Jake: "some little greeting character that waves inside like a gray box … to the right of welcome" → the EA mark, not a character — Huge's plush H, Lando's bee: brand objects doing one small living thing, a cartoon reads as a chat widget). A grey square (surface-1, the frame's radius) on the column's right, exactly the band's height (the day line's top to the door's bottom — measured live via getBoundingClientRect, not a CSS calc against the stretched grid row: a grid item's child margin does not collapse through it the way a plain block's does, and pulling the gap back out with percentage math left the aspect-ratio-derived width a subpixel off the "auto" column's own reserved one, enough to trip the ±1px right-edge check at 1440×900 though not at 736), the mark white at 44% inside; ONE wave on load (rotate −14/+12/−6 → 0 over 1.1s on the reveal curve, pivot low at 50% 70%, starting 900ms in), a 6s breathe on the box (1 → 1.02, unconditional, never gated on the entrance), and a 6px lean toward the pointer normalised against the box's OWN half-size so its own corner reads exactly ±1 (Lando's bee idles toward the cursor). Links to /work; hover steps the box to surface-2 (300ms). Hidden on the phone — a 96px grey square on a 390 screen is noise the wave doesn't earn there. Reduced motion: still (rotate/scale/translate all neutral).
- 2026-09-20 · §02 · THE STATEMENT IN THE MODEL'S FORM (Jake: "the section after the video, do you think we should make it more like the reference, not the snows one, the main one" → "let's change those then"). Huge's own decode (decodes/hugeinc.md §2/§4): 145.65px/600, sentence case, left, three lines, one 57.46px paragraph. Ours: sentence case, one weight (600), LEFT on the column — the centred, condensed-caps, uppercase 101px statement (danielsnows' own shape, 2026-09-14) was the last of the previous identity left on this page; `font-size: min(8.2vw, 7.4rem)` (118.08px at 1440) so our 52-character line makes three lines the way Huge's own 36-character line makes three at 146 (the two blocks measure the same height); the rotating word and its cyan underline (2026-09-19, THE ROTATING WORD/THE ACCENT) are unchanged in mechanism, just smaller — h2 height holds constant (325.88px) across a whole 11.7s lap regardless of which word is showing. ONE sentence under it at the 43 rung, left — `"Built by the person you talk to — a fixed quote in two days, a price that never moves."` — the old three-sentence 27-word paragraph (2026-09-18) is gone: THE LAW OF THE MODEL (2026-09-19) already says at most one sentence under a section's line, and THE CONVERGENCE's "the paragraph under it cut" was the wrong reading of that law for a section whose own reference (Huge) keeps a one-line paragraph. Measured at 1440: h2 left 54.7 (the column), exactly 3.00 lines (height 325.88 ÷ line-height 108.63); paragraph 43.2px, 2 lines. At 1280: 3.00 lines at 104.96px. Phone (≤900): `clamp(2.4rem, 11vw, 3.6rem)`, left, paragraph 20px. THE EYEBROW GOES, mid-step (Jake: "i want eyebrow gone in section after video") — `.dr-offer-kicker` ("What this is") is deleted from the markup and its CSS rule removed: Huge has no eyebrow above its statement either, and it was the smallest text left in the section, exactly what THE LAW OF THE MODEL says a homepage section should not carry. THE SETTLE IS ALSO GONE, mid-step (Jake: "i dont like the shrinking of that title either") — the `1.12 → 1` scale on `--cur` and its `transform-origin` are removed from `.dr-offer-say`; the words are STILL the whole way, and the film leaving is the only thing that moves in the reveal. The curtain's pin, the `--pre` opacity gate and `--say-hold` are all untouched; `--cur` still runs 0→1 on `.dr-say-stage`, nothing reads it now. `.dr-say-pin { overflow-x: clip }` stays (harmless, and was already covering the settle's old overshoot).
- 2026-09-20 · hero/snows · THE LIVE TILE (Jake: "still feel theres something a little more we could do with the right side" → three mocks → "lets do 1"). The mark's box widens to the band's right half and gets a JOB: real, ticking information on its left — the Phoenix clock (the visitor's clock in our time zone, ticking), the reply line derived from the same day logic as the greeting (within the hour / tomorrow 8 AM / Monday 8 AM …), and the honest capacity line (NEXT_START, only while set) — the mark large on its right, still waving once, breathing, leaning. Lando's nav clock and Huge's showreel tile are the precedents: a tile that carries something real. No accent in the tile — the band already has two cyan objects. The day line wraps into its half. Phone: hidden. Measured: `.dr-hero-band` grid `minmax(0,1fr) minmax(0,1fr)`; tile right edge 1385.3 at 1440 (±1 of the column's own), left edge = the band's midpoint + 16 (736 at 1440); `.dr-hero-mark-m` fixed at 72px (not 44% of a square any more); the LEAN moves the mark alone now — page.tsx measures/writes `--lx`/`--ly` on `.dr-hero-mark-m` itself, not the tile, since leaning a tile of live copy read as a bug, not a bee — the wave and breathe are otherwise unchanged in kind (breathe now on `.dr-hero-tile`, the renamed box). Removing the day line's `white-space: nowrap` (it held one line only while the tile was a small square and the day line had the column's full ~1330px) makes the longest greeting wrap to 2 lines at 1440/1280, growing the band from 119.4px to 164.8px (736) / 169.7px (900) — verified this still clears the film: slot's top stays ≥ the door's bottom + 19 at 736 (the pre-existing margin-top floor engages, 597.4 vs 597.3 required) and still lands exactly at fold − 200 at 900 (700.3 vs 700) — the 22ch fallback cap was not needed.
- 2026-09-20 · hero/snows · THE BAND IS ONE CARD (Jake: "extend the card, i want to see it wider where the text on the left and everything is inside of it"). The live tile widens to the whole column and the band's contents move inside it: the day line (the cyan day word) and the cyan door on the left, a divider, the live clock / reply / capacity lines, the mark on the right — one surface under "Welcome", the content on a panel (Jake's own law). The breathe drops to 1.006 on a card this wide; the wave and the lean stay on the mark. The film keeps its height under the card. Phone: the card, single column, the mark hidden.
- 2026-09-20 · §01 · THE PERSONAL MESSAGE IS THE TITLE — trial branch hero/greeting (Jake, six rounds in one session): the PERSONAL MESSAGE is the title — the big thing (Jake); the claim is what a first-time visitor is told; from the second visit the title is the site recognising you, the day line under it always. What shipped, in order tried: (1) the greeting itself became the h1, retiring "Welcome" and its canvas measure; (2) "maybe we have no bar, bigger text" retired the band card outright (its CSS/JSX kept dead, not deleted); (3) "can we get the gray box around lets talk, have it be the cta" promoted the day line's second sentence into the title as a grey link; (4) "i need to find a different way to show its clickable" stroked that box and added a sliding arrow, and measured the air around it; (5) "i first just want the basic one" — a first-time visitor needs the CLAIM, not a greeting; (6) Jake corrected (5) as inverted: the greeting IS the big personal message, the claim is only what a first-time, no-signal visitor is told. (2)–(4)'s CSS (`.dr-hero-band` and its columns, `.dr-greet-cta` and its arrow) all stay in room.css, dead, not deleted, per this branch's own convention.
  THE SHAPE THAT SHIPS: `Greeting` is `{ title, sub, door }` (lib/greeting.ts) — `title` is the h1, `TITLE` ("Sites, automation and ads for owner-run businesses." — shortened from §02's own "Websites, automation and ads for owner-run businesses.": shorten copy, never the size, per this step's own rule, measured live at three lines before the cut) on a first visit, the escalating return-visit line from the second (cyan lead via `splitLead`, exported — the claim has no ". " to split on, so it falls through with no cyan by construction); `sub` is the honest-capacity day line, the SAME at every visit count; `door` is its own pill again. `pickGreeting(visits, persona, now)` already takes a `Persona` (lib/persona.ts's own `?i=`/`?svc=` shape) for a PERSONA TABLE that is designed, not built — every first-time visitor sees the same `TITLE` regardless of persona today.
  THE ARCHITECTURE, forced by the requirement that the server render the real title (no swap on a first visit): `app/page.tsx` is now a genuine Server Component (`export const dynamic = "force-dynamic"`) that calls `pickGreeting(1, {i:null,svc:null}, new Date())` and hands the result to `components/dark/hero-room.tsx` (the old all-in-one "use client" page, renamed, taking `initialGreeting` as a prop). VERIFIED, NOT ASSUMED: `export const dynamic` declared directly on the old "use client" page.tsx built as `○ (Static)` regardless (`next build`'s own route table, checked twice — before and after the split) — a "use client" page file does not honour Next's route segment config, so without the split, "today" would have frozen at build day. THE CROSSFADE (Jake: make the return-visit swap read as recognition, not a glitch): a `swapped` state arms `.dr-greet-swap` (400ms opacity, `.dr-lit .dr-greet.dr-greet-swap` — a real bug found live, `.dr-greet-swap` alone lost the cascade to `.dr-lit .dr-greet`'s higher specificity and the crossfade silently never played until the selector was fixed to match).
  TWO MORE REAL BUGS FOUND MEASURING: (a) `.dr-hero-door`'s inherited `.dr-herocta { margin-left: auto }` (written for a flex ROW) pushed the door to the right edge once it became a plain column child again, off-centre (measured 1291px, not 720) — fixed with `margin-left: 0` on `.dr-hero-door` itself. (b) the film's own floor (`clamp(16px, 2.6svh, 32px)` on `.dr-slot`'s margin-top) gave only 19.13px of door→film clearance at 736 against the spec's own "≥ 24px" — 2.6svh alone is 19.14px at that height — raised to `clamp(26px, 2.6svh, 32px)`; the 900 number (calc-path, 113px, unaffected) still lands `--slot-show`'s own 300px almost exactly (299.9).
  MEASURED, ALL FOUR TITLES, 1440: 118.08px (`min(8.2vw, 7.4rem)`), `max-width: 22ch`, exactly two centred lines each (claim, "Back again.…", "Third visit.…", "You keep coming back.…"); centre 720 at every width tested (1280/1440/2560), 640/720 respectively. Gaps at 736: nav→title-cap 50.77px (≥ 40), door→film 26px (≥ 24, the raised floor); at 900: nav→cap 62.09px, door→film 90px (calc path), film shown 299.9px (~300, target). `--grow` 513px (736) / 600px (900), reel lands `[0,0,vw,vh]` (±1) at both. Nav sentinel now targets `.dr-hero-door` first (chain: door → band → title → old day line, so every shape this hero has had today still resolves to something) — sentinel height 487px, door bottom 486.25px, flip at scroll 486→490. Server HTML confirmed containing both the title and the day line (curl). Phone (390): title `clamp(2rem, 9vw, 3rem)` (35.1px, ≤ 3 lines — the claim itself makes 3), sub 20px, door full width, no card. tsc clean throughout; 0 console errors at 390/1280/1440/2560/900; `next build` confirms `/` as `ƒ (Dynamic)`.
- 2026-09-20 · §01 · THE VOICE OF THE HERO (Jake: "i dont want to be too focused on conversions, all those messages are more for conversions, i want this to be fun … the first visit needs to say what we are, all the others can be fun" · "lets not use theirs word for word, i want to capture the feel of theirs without their exact ideas" · "maybe we dont do daily"). Huge's own lines were pulled (decodes/hugeinc.md §3) and two of ours were theirs nearly verbatim ("Working on a Saturday? Respect", "Mentally, Fridays are the weekend", "You again. Excellent") — all replaced. What ships, none of it theirs: first visit "We build the site. You get on with the business." (says what we are, with a wink); 2nd "Back so soon. We like you already."; 3rd "Third time? We'll have to start charging rent." (the longer cut made three lines at 118 — copy shortened, never the size); 4th+ "Oh, it's you. Come in, come in." The day line only on the days that earned one — Mon "Nobody asked for this." · Tue "Monday's less annoying little brother." · Fri "Whatever it is, it can wait till Monday." · Sat "You're working? Go outside." — Wed/Thu/Sun quiet, the title and the door alone; the reply-time promise left the line (the door and the nav carry the practical). The greeting is re-picked on the visitor's clock every visit (the server's day line was the server's day). The persona table (?i=/?svc= first-visit titles; the flying line is in the drawer for the aviation persona) stays designed, not built.
- 2026-09-20 · §01 · ONE LINE, NO DAYS (Jake, on the live hero: "i think the one line is clean, screw the days, maybe we can add personalization throughout the site too"). The hero is the title, the door, the film — nothing else. The four titles: "We build the site. You get on with the business." (1st) · "Back so soon. We like you already." (2nd) · "Third time? We should start charging you rent." (3rd) · "You again. Don't you have a business to run?" (4th+ — Jake: "i really like the business to run but need to change it a little" — the exact wording still his to tweak). The day table stays as an empty map (one edit brings a line back). Personalisation is now a page-wide idea, not a hero one — the persona table (?i=/?svc=) and a returning visitor's traces elsewhere (the rail leading with the case they opened, the ledger leading with the service they came for) are the next design, not built.

## 2026-09-20 · THE ROTATING WORD IS CYAN ON WHITE

Jake: "the underline words i want to just be the color." The statement's rotating word (§02, `.dr-offer-rot-w`) is set in the accent itself on the white page — cyan letters, no underline. This is Jake's own, knowing reversal of the 2026-09-19 accent rule ("never cyan letters on white") for this one word: it measures 1.5:1 against the page, and it is the brand's colour said out loud, not a word that has to be read cold — the sentence carries it. The rule stands everywhere else; `--accent-off` (petrol) remains the answer for a word that must pass.

## 2026-09-20 · THE AIR — what settled on branch hero/air (46 commits, pushed)

Jake, at the day's start: "theres something simple missing in the hero … maybe some way to always have the text be full width, or maybe theres some sort of ambience missing." What the frames settled, in order:

- **The title fills the column** (`wide`): one size, balanced, the longest line fitted to the measure by measurement (131 on the claim, 179 on "Back so soon.", 136 on "You again."). Not one-sentence-per-line at its own size (`lines`, built, rejected: "i like the previous better").
- **The film at rest is edge to edge less 10px** (`full`), rounded; it grows to the whole screen (square) and scrolls off with the picture sliding 7.5% inside the mask on its own exit clock (`--x`); NO overscan — "i like getting rid of exit slack".
- **A cyan glow on the film's edge** (`edge` + `cyan`), biased up, the sides "almost invisible"; a mirrored glow under the film's full-height bottom edge, and **26svh of black** under the film before the page. The lamps (`light`), the halo (`halo`), the frost on the statement (`glass`) and Huge's nav blur (`pblur`) were all built and turned off — the frost lives in the RAIL instead, as each case's caption strip (`frost`).
- **No curtain** (`flat`); **the glass on the seam** (`seam`, 220px, symmetric progressive blur on the black/white line — the "more dramatic" version that put the film inside it was reverted).
- **The scrawl** (`scrawl`): the section's nickname hand-written in the accent behind its head, writing itself on scroll — Lando's Rive "Collabs" decoded; ours in Caveat Brush until Jake writes the words himself.
- **The wall (the background mark) is gone.** **The services card is off the homepage.** **The rotating word is cyan on white** (above).
- **§02 is Huge's second beat on leoparpeix's screen**: §02a one screen — Websites / Automation / Ads for / [audience] at 138, one a line, the masked rise every time it enters, the corners (Mesa · building for anyone · Phoenix clock · reply hours · capacity); §02b the paragraph at 57 with the four doors on the right, on the wipe.
- **The page flips to dark before the work, scroll-driven** — the room's own `--flip` on the root over a fifth of a screen (a longer window measured grey-on-grey at its midpoint); everything after the work sits on the void. The nav reads the root's `--flip`.
- **The rail is continuous and flat** — steps, snap and depth squash off; the pictures keep the horizontal parallax and gain a vertical lag into and out of the pin.
- **`?v=` is the trial switch** (app/page.tsx: tokens add to the default set, `none` clears it); the winners are the default. Folding them into plain CSS is housekeeping still owed.
- **Pipeline law**: never write `-webkit-backdrop-filter` in room.css (checks.md 2026-09-20).

## 2026-09-21 · THE PAGE IS DARK

Jake, after the scroll-driven flip put the work on the void: "hold shit the dark version looks incredible im debating if we even have the light section" → "i think i like the dark page better than white section." The room is dark end to end again — the hero, the screen, the paragraph, the tiles, the work, the close. This is Jake's own reversal of 2026-09-19's "white page, black hero" (the model's structure); the accent's happy case everywhere (cyan letters on dark, 12.8:1), and the rotating word's cyan-on-white exception (2026-09-20) has nothing to except. `?light` is the comparison. What the light page's seam owned — the glass on the black/white line, the 26svh of black under the film, the flip track before the work — is inert on a dark page and stays written; housekeeping may remove it.

## 2026-09-21 (evening) · PAPER AT THE WORK, LANDO'S WAY — AND THE PIN STAYS

- **`?v=paper`** (commit 0e0a2ec → 406c33a): the ground lifts dark → off-white ACROSS the work rail, scrubbed, measured on landonorris.com (olive 40,44,32 at his pin's start → 244,244,237 by 90% of his rail); ours over the first 60% of the travel (2.5 screens — Lando's own distance, after "make the fade slower like lando"); the voices, the runs and the objections on the light tokens; the close takes the room back to dark. Two clocks (--fp, --fe), one typed --flip = min(); the ink crosses in a tenth of the window; the lamps go out with the dark. Jake: "i like it". Reason it was proposed: six of the rail's nine pictures are dark UIs (mean L 20–74) — dark-on-dark reads as atmosphere, dark-on-paper reads as a product.
- **`?v=nob` / `?v=noc`**: the page without §02b (paragraph + doors) or §02c (tiles). Both built to be looked at; neither decided.
- **`?v=gallery` REJECTED as the work** (1b784fe — Apple's "Get the highlights." gallery in the rail's place, every number his): Jake, "i like previos version. i miss the pin." The rail's pin is load-bearing — it is the one place the page holds you. The gallery stays behind its token; what survives of the Apple decode for this page is the MOTION law, not the object: one curve (ease-in-out, ~1s, lands exactly), text after picture ((1−|d|)³), one thing moving per interaction.

## 2026-09-21 (late) · THE HERO IS THE TITLE AND THE FILM

- **`nopill` is the default** (Jake: "i like nopill", after the strip came back into this hero — `?v=strip`, liked, then "the nav area looks weird … the top half spacing is odd", the left-aligned title, the strip moved into the rail — `navstrip` — and "no pills on the hero at all"). Nothing under the title: the film follows it 43px down, 470px of it in the first screen. The nav stays quiet at rest and becomes the pill with the call on the first scroll. The door and the proof line are OFF the hero; the price door lives in §02b's doors and /pricing, the rating in the tiles until Jake places it elsewhere. Centred title (the `left` token stays available).
- Still tokens, not decided: `paper` (liked), `lm` (seen), `strip` / `navpill` / `navstrip` (seen, passed over), `nob` / `noc`, `gallery` (rejected).
- **`navc` is the default** (same night — "do you think nav should be centered instead of wide" → "i like it when its in the scrolling state not the original"): the rail is one centred cluster on the hero's axis, and it is the PILL from the first frame — mark, links, the call — not something the first scroll earns. Reverses 2026-09-13's "links sit right", knowingly: that was made for a left-aligned hero. Site-wide follow-up owed: the interior pages' rail.
- **`logoc` is the default; the pill at rest is not** ("i dont want the pill in by default i do like it centered tho" → "in the default i want the logo in the middle at rest and when scrolling i dont want it in the middle"): at rest the rail is Work · Services — the wordmark — Pricing, one centred cluster with the mark at the page's centre; on the first scroll it is the pill it always was — mark left, links, the call right. `ask` (the chat's one entry, with the links; the panel drops from under the rail) is built and seen, not yet default — the panel's skin and the production API key are owed first.

## 2026-09-22 · THE HERO'S SPACING, SETTLED ("this looks good")

- **`tidy` is the default.** Measured at 1440×900: the film on the title's column (gutter = the column's inset and cap; it had been 19px wider than the words each side, 96px at 1920); 52px from the nav's row to the title's cap line, 72px from the baseline to the film (Jake: "the title and video are really close" at 40 → "focus on video" → 72); the nav a step larger (links 14, mark 24, 40px between the groups, 24px from the top). The title's line masks carry .16em of room for the descenders (the g of "get" had been cut flat) with the riser starting a line plus that below; adjacent lines' negative margins are summed (−.32em) because they collapse — a flex column stopped the collapse and broke the fit (ten lines).
- The rail's regroup (rest cluster → pill) is a FLIP move on one curve, released in the same frame as the pill's own transitions, with the call's margin and measured width on that curve too; three measured dips in Work fixed on the way.

## 2026-09-22 · TWO ROOMS, BOTH REAL

Jake: "i like the first two sections hero and 2a, also the light version, im hoping that can be improved, im invisioning developing light and dark, i change my mind constantly over which i like more."

- **The light page is a ROOM now, not a trial.** `.dr-root.dr-day`, chosen by a switch in the rail, remembered in `localStorage` (`eas-room`), applied to `<html>` as `eas-day` by an inline script in `layout.tsx` before first paint — verified: a reload in the day room paints `rgb(231,231,234)` on the first frame, no flash. `?v=lm` still works as the URL way in. The rail's chrome follows the ROOM, not the old token (a real bug: the switch left a dark pill on a light page).
- **THE DAY ACCENT `#00768f`.** Measured, every colour in the light room cleared AA except the accent: electric cyan is **1.24:1** on the off-white ground (12.85:1 on the dark one), so half the headline read as a highlighter. Same colour, darker — identical hue and chroma in oklab (H −150.4°, C .143), L .846 → .50 — which is Apple's own answer (#2997ff dark / #0071e3 light). **4.27:1** on the ground, 5.27 on a white surface. Candidates rendered at 131px: #008fa8 (3.10, large-text only), #00768f (4.27, chosen), #005e77 (5.93, closer to navy).
- The hero's cyan edge glow drops to a third in the day room — a cyan haze on off-white reads as a smudge where on black it reads as a lamp.
- **`hud` is the default** (2026-09-22, on itsoffbrand.com's bottom-right corner — measured: a 45×46 burger 19px off both edges, a 71×23 light/dark toggle beside it, a 229×440 menu opening upward over ~800ms, all of it ON TOP of a full top bar, so the corner holds TOOLS, not the path). Only the room switch goes down there; the rail is the nav Jake asked back for — "just before the lightmode switch": Work · Services · the mark · Pricing · Ask at rest, the pill with the call on scroll, Ask keeping its island. `?v=hudask` is the fuller corner (Ask down there too, the chat growing upward out of it), built and not chosen.

## 2026-09-22 (late) · THE MARK WRITES ITSELF

- **`logopen` is the default.** Hovering the nav's mark writes the accent through it along a hand-authored centreline (`mark-svg.tsx`, PEN) used as a MASK over the real glyph — so the shape is always exact and only the writing order is invented; 1.1s to write, .38s to wipe. Jake: "i like it".
- **Why it took six tries, for the record.** Lando's own nav logo — the thing being asked for — is a **Rive** animation (`<canvas data-rive-ln4>`, a state machine); his wordmark does nothing on hover at all (measured twice). And our monogram is ONE filled path with no parts, so nothing inside it can be animated: a sweep, a glitch, a corner flood and a contour trace all failed for the same reason — there is no structure to move. The answer was to stop animating the glyph and animate the ORDER IT ARRIVES IN.
- Measured laws that came out of it, now in `checks.md`: SVG restarts the dash at every subpath; an SVG path with no `fill` rule paints solid black; an unscoped `.dr-lockup:hover .dr-mark-*` runs under every token; librsvg ignores `pathLength`, so mark animations must be measured in Chrome, never in a static render.
- Rejected on the way, all still behind tokens: `logoscan`, `logoslice` (too small to read at 24px — a scale-up made it legible and Jake rejected the scale), `logofill` (a level rising — "weird"), `logowipe` (a diagonal wavefront — "just going diagnol"), `logoink` (a contour trace), `logoname` (the name unpacking), `logobloom`.
- **`?v=shade` — ambience on paper** (2026-09-22, "do you think there is a way to do the ambience in the light mode"). The dark room's rig cannot cross: a glow is ADDED light and the light ground is #e7e7ea, 231 of 255 — 24 levels of headroom, so the brightest lamp reads as a smudge. On paper ambience is SHADE: the same rig, the same drift, the same off-for-the-screen clock, but each lamp a soft cool darkening tinted with the accent's hue instead of a warm brightening. Depth had to go up an order (there are 231 levels below the ground against 24 above): at the glow's own alphas the ground moved 230.3 → 228.1, invisible; at .10–.16 it lands 231 → 222 at the cores. The film's edge light becomes the film's shadow — an object that dark casts on paper, it does not glow.
- **`shade` v2 — black and grey, and the sheet itself** (same day, "it looks pretty similar … maybe we have to use blacks and grays. the blue is so hard to see"). Right on both counts: at any alpha soft enough to BE ambience, a blue tint on #e7e7ea is a colour nobody can name — the eye reads the darkening and misses the hue, so the hue only costs saturation. The lamps are neutral black now, and two things were added that a dark room never needs: a fixed vertical/horizontal shade on the sheet (231 in the middle, ~223 at the edges, so the page reads as a lit sheet rather than a colour) and 2.4% monochrome grain from an inline feTurbulence (no request, no decode) — on the void the film and the glows carry the texture; on paper there was nothing, and large flat areas banded. Measured: ground mean 230.5 → 219.3.
- **§02a × §02b — why they don't sit together** (2026-09-22, "i really like section 2a and 2b individually im not sure they each go together"). They come from two sites: 2a is leoparpeix's centred poster (138, one word a line, rises on entry, lamps off) and 2b is Huge's second beat (57 on the left, doors at x 1239, a scrubbed sweep, lamps on). Each is right because it is faithful to its source. Back to back they switch axis (centre → left edge + right edge), scale (138 → 57 is neither a caption nor a peer) and motion language in one scroll, and at the handoff (y 1770 at 1440×900) they share a single frame. Two trials: **`?v=answer`**: 2b written in 2a's grammar: centred, the 43 rung, balanced lines, the doors as one centred row, the sweep untouched, so poster → sentence → doors → tiles sit on one axis. **`?v=apart`**: neither changes, and 2b moves to after the work (the promise lands after the proof, "See the work" door dropped, the tiles carry the --son lamp clock so there is one writer). apart only half-fixes it: 2b then shares a frame with the voices' centred head, the same clash one section later. Recommended: answer. Not decided.
- **`?v=show` · say it, then show it** (2026-09-22, "i need to find a way to make it beatiful and visual"). The poster names three things and until now nothing on the page showed any of them; the next beat was more words. Between the poster and the sentence there are now the three photographs that already existed for exactly those words (public/services: Desert Wings' site on laptop + phone, the reply that booked Saturday 9:00, the search they won), in a bento on the column: Websites big (876×650 at 1440, ~4:3, so the laptop is shown whole), the two phones stacked at 3:2 (438×292). The captions are the poster's own words, each with one line and an arrow to its service page. It is NOT the services section that was cut (2026-09-20): no prices, no rows, no deliverable lists. Motion: one derived settle per well (scale 1.08 → 1 as its top goes from the fold to .35, lerp .1), then still. Meant to go with `answer`: `?v=answer+show`. Two first-cut bugs: a 1.21 box cover-cropped the laptop's base, and an arrow pushed to the card's edge read as the next card's. On a phone a no-break space did not keep the arrow with its last word, because Chrome breaks before an inline-block; a nowrap span does.
- **`?v=survey` · the ground, step 1 of the Lando plan** (2026-09-22/23, "yes" to design-dna/lando-ground-plan.md). One WebGL2 contour field (components/dark/survey.tsx) under every section. It morphs in place through a time-warped fbm and never slides as a sheet (his 09-22 complaint about the cursor parallax). It travels at 1:10 of scroll, his hero ratio. Each section declares its ink and drift in one table (his `data-gl-change-from/to` idea), §02a is drift 0, and the lines are plain hills, not the mark (09-20). CALIBRATED on his own census frames, full-res: his lines sit ~+12 over olive and ~−40 under cream and change 0.9–1.8% of an empty crop per 700ms. Ours sit +25 on the void (set a little brighter because the room's vignette eats the edges) and −33 on paper, and change 1.5–2% at rest. The first cut (K 6 / 620px / 3 octaves / α .055) was +2 levels, wiggly and sparse: invisible. VERIFIED: the words are never crossed, §02a is 0.00% still, the seams are smooth, reduced motion is one still frame, nothing breaks without WebGL, +8ms LCP, 0 long tasks, 30fps at rest and 0 when still or hidden, phone capped at 1.25 DPR. ONE MISS, EXPECTED: on the coarse census method (360×225, >12) the day room reaches 8/31 stops, split 4/3/1, and the dark room is unchanged at 4/31. That method cannot see tint-level lines, and it reads Lando's own olive field as 0.00 too; at full res the field is there (day 0.3–1.2%, dark 0.1–0.55%). The back third is carried by the strips (step 6) and `paper` (step 7), exactly as the plan's coverage count said. Trial, not default.
- **`?v=survey+mark` · the lines drawn from the mark** (2026-09-23, "i am worried though of it being too close to lando" → "yea just to see it, but i really like it now so make sure its committed"). Plain `survey` is unchanged and committed. The honest answer to "too close": the technique (a faint self-moving layer) is not his, and neither is our code or pattern, but faint contour lines on a dark ground are his most recognisable texture, and his mean something (his helmet's swirl) while ours were generic hills. `mark`: the height becomes the EA mark's signed distance field (a 512² fill of MARK_D, an exact 2-pass EDT, two box-blur passes to take the texel stair-steps out, R16F). The rings run 22 per mark width, the mark box is 0.85 of the screen wide and centred at 58% at scroll 0, the same time warp at 110px, and 18% of the hills ride on top. The rings echo the E and the A as offset outlines near the glyph and round off into hills farther out: engineered where his is organic. It is NOT the wall (09-20): no fill and no outline, only lines. Token only; Jake's choice between the two.
- **`?v=survey+loose` · the mark's mass, not its letters** (2026-09-23, "i like the hills better it feels too tight right now, any suggestions"). The tight mark version reads as a drawing of the letters: rings packed along the strokes. Loose blurs the distance field until the letters are gone (box radius 14 texels ×3 passes) and leaves only the mark's MASS. The rings run 17 per mark width over a box 1.15 screens wide, with a 170px warp, and 45% of the hills are back on top. It reads as calm as the hills, with the same line density and slightly rounder, blockier forms: a landscape that swells around the mark instead of a picture of it. Offered as the in-between; plain hills stays the version Jake prefers, and `mark` is kept as a token.
- **`?v=aud` · the audience ticker, step 2 of the Lando plan** (2026-09-23, "keep the hills, go to step 2"). The hills are the field's default, and `mark`/`loose` stay as tokens only. The ticker is his message ticker: the six audiences (OFFER_ROT_WORDS, capitalised, no full stops) in two rows running opposite ways, in the band between the film and the statement. It is 92px Archivo 600 at 1440 (his row height) and 28px on a phone (never bigger than the hero title). The first row is ink-3 at 45%, the second ink-3; full bleed and unmasked like his; the space is the separator. ONE RUNNER for every strip to come (components/dark/ticker.tsx): four clones per row, one JS translate, 21px/s drift (Jake's standing marquee speed) whose sign latches to the last scroll direction, plus ±10% of the viewport width pushed by the scroll, crossing 0 at mid-screen. It runs only on screen and with the tab visible; under reduced motion it stands still. It lives INSIDE `.dr-say-stage` because the default `flat` token keys the stage on `.dr-hero-wrap + .dr-say-stage`, and a sibling in between would break that geometry. The field's §02a stillness moved from the stage to `.dr-say-pin`, so the ticker band stays alive. Measured: a 21.0px/s drift at rest, scrollWidth 390 on the phone, reduced motion `none`.
- **`?v=hold` · the statement holds, step 3 of the Lando plan** (2026-09-23, "yea show me step 3"). This goes against the rotating word (09-19, Huge's), so it is a token and Jake's call. Lando's statement lands and holds (his y 1800 reads 0.00 at rest). Ours swapped its last word every 11.7s, which put the message itself in motion, and since `aud` the same six words already run in the ticker above. Under the token the timer stops and the statement shows ONE word: the visitor's own audience when their ad named one (lib/persona.ts `?i=`, matched loosely: flight/aviat → flight schools, coach/pickle/fitness → coaches, clinic/health/dental → clinics, nonprofit/charity/museum → nonprofits, contract/plumb/roof/hvac/trade → contractors), otherwise "owner-run businesses.", the true sentence. The screen reader hears the word that is shown. Measured: one word at opacity 1 and the rest at 0 for 4s straight; `&i=flight-school` holds "flight schools.".
- **`?v=roll` · small hovers everywhere, step 4 of the Lando plan** (2026-09-23, "yea show me step 4"). His hovers, measured: the STORE pill rolls its label (a two-copy roll, settled by ~520ms), and every CTA arrow is a Rive arrow that flips through an edge-on sliver in 150–300ms. Ours: "Book the call" in the rail takes the rail's own per-letter Roll (the links already use it, so the rail has one hover, not two), with a real aria-label because the Roll is aria-hidden. Every arrow inside a link (`<i aria-hidden>` → and ↗: the doors, the show captions, the rail's See the case and Live, the close) flips instead of nudging: ease-in to 90° by 160ms, round the other side, and land on (.19,1,.22,1) at 380ms. Measured: the arrow's width goes 19.3 → 3.8px at 165ms → 17.7 at 200ms. NO perspective: with perspective(12em) it read as the arrow shrinking, not turning edge-on. Hover-capable pointers only, and nothing under reduced motion. A dev-server gotcha on the way: Turbopack served the old keyframes until room.css was nudged (the known stale-CSS ladder).
