# Decisions — /dark (append-only)

This file is APPEND-ONLY law. Never edit or delete a line below — if a new
step would require reversing one, that is a CONFLICT: stop, do nothing,
and surface it (to the main loop, and to Jake if needed) rather than
silently overriding a prior call.

- 2026-09-06 · seams · five seams get five distinct devices, never the same mechanism twice in a row (law 1) — hero→§02 sticky-cover climb, §02→§03 fade+whitespace, §03→§04 whitespace-only seam + repeated opener, §04→§05 the page's one ground flip, §05→§06 bookend dome mask (ffd1ec9, 7d8f466, 2a9bb7a)
- 2026-09-06 · §02 · surface stays WHITE (#f5f5f7 + original inks) — light gray and true mid gray were both tried and rejected ("the original whiter is better"); do NOT re-propose gray for §02 (bb2627b)
- 2026-09-06 · §02 · service-shot clip-open + parallax removed, shots rest STILL (saturate .25, colour only on hover) — "the animation on the images… drags my eyes to it"; a whole ROW may still grow as one unit under the spotlight and carry its still image along (cdc6f4c, amended 6b886f2)
- 2026-09-06 · §02 · lens+ride: --spot is a raised cosine of the row-centre's distance from the 60vh reading line (reach 1.5× row height, no plateau), chased 0.1/frame; row scales 4.5% from its BOTTOM edge; --dy per row rides the list's lift plus the growth of every row below it — nothing ever moves down (0a2c6a4)
- 2026-09-06 · §02 · row hover removed entirely; the arrow-slide and colour state now ride --spot/is-now instead of :hover (eaf00a2, 0a2c6a4)
- 2026-09-06 · §02 · never grow a large surface via clip-path/inset/radius on scroll — the room-fill light is a CONSTANT box scaled on X only, radius pre-stretched (fb13710)
- 2026-09-06 · §03 · nothing composited at rest inside a translating card — no backdrop-filter or live clip-path'd video while a card is still moving through its entrance; blur is a `filter` on the cover image, hover-only; the demo panel is `visibility:hidden` until hovered (efc50c6)
- 2026-09-06 · §03 · the picture covers only the VISIBLE part of its well, and is NOT chased — well is 3:2, img top -19%/height 126%, translate driven directly off --pp with no lerp of its own; a 0.1 chase's lag exceeded the well's margin at wheel speed (cf7ad97 → 140678b)
- 2026-09-06 · hero · the hero roster keeps its small hover-clips as the through-line motif alongside §03's cards, unless Jake feels it read as a repeat once he reviews (0050b34)
- 2026-09-06 · global · Lenis IS mounted on /dark, via the root layout's SmoothScroll — desktop wheel only, lerp 0.1; no section's scroll math should assume it is unsmoothed or that a local chase is standing in for it (correction noted at eaf00a2)
- 2026-09-06 · §04 · the walking light and the never-settling drift are REMOVED — the grid's window is a one-time ARRIVAL (data-sp-from 1 → 0.62), every panel whole by grid-top ≤.73vh, nothing answers scroll after that point; §04 is the back-half's still beat (2392de6)
- 2026-09-06 · §04→§05 · the page's ONE body-ground flip is declared on `.dr-obj`: void → surface-2 (#171719); the ground holds to the page end and never flips again (2a9bb7a)
- 2026-09-06 · §06 · `.dr-close-rig` has no `data-sp` and no lag translates — §06 is rigid, deliberately (law 12) (2a9bb7a)
- 2026-09-06 · headings · one triggered vocabulary only — `data-wipe` line-wipe on every heading across §03–§06; no second triggered vocabulary gets added without retiring this one first (f01453c)
- 2026-09-06 · §05/§06 · SUPERSEDES the §05→§06 dome-mask seam (line 8) and "§06 is rigid" (line 20), on Jake's ask: the ending is rebuilt to the Off+Brand sequence (design-dna/decodes/offbrand.md) — §05 becomes a WHITE card that recedes (scale only, constant radius, native scroll via data-sp so it runs on touch) to reveal the lifted ground, and §06 is the CTA + footer as ONE section on that ground; the rig/glow/dome/mark bookend is retired. The ONE ground flip (line 19) stands. New seam device = object-recedes-to-reveal-ground (distinct from the other four).
- 2026-09-06 · §05/§06 · shipped numbers for the card-recede + CTA/footer room: `.dr-obj-card` scale window is bottom-edge 0.6→0.3vh, lerp 0.12, scale 1→0.94, radius clamp(22px, 2.2vw, 32px) constant; `.dr-close-h` is sized to its OWN column via `container-type: inline-size` on `.dr-close-say` + `font-size: clamp(2rem, min(1rem + 5.4vw, 12.6cqw), 6.4rem)` — not viewport vw — after the original vw-only formula wrapped 3 of 4 lines at 1440 against the given 7fr column; copy shortened to "How about" / "a site that books" / "for you, [pill]" / "while you work." to fit.
- 2026-09-06 · kickers · NO chapter mark above section titles: the `::before` monogram on the §03/§04 kickers is removed on Jake's call ("the little logos above the section titles don't make sense"); the mark appears only in the nav lockup and the §06 closing lockup
- 2026-09-06 · §04 · SUPERSEDES the staircase (four panels each a tread higher, treads + risers drawn under them) on Jake's call ("the staircase thing is ugly"): the four steps sit in ONE ruled slab — one flat surface, four cells, hairline dividers, one ruled line under the station numbers that lights 01→04 on the arrival; no per-cell lift, no risers, no backdrop blur. The arrival window and per-cell stagger (line 18) stand. (ba6ad74)
- 2026-09-06 · §03→§04 · SUPERSEDES "whitespace-only seam + repeated opener" (line 8) and the §04 window numbers in line 18, on Jake's ask ("something dramatic … motion or movement wise"): the seam is a CURTAIN LIFT — §03 is an opaque black surface that translates up by --seam-lift (32vh, ≈1.5× scroll) as its wrapper's bottom edge crosses 1vh→.32vh, uncovering §04, which is now a LIGHT ROOM (#f5f5f7 foreground plate, §02's ink tokens, white ruled slab) overlapping §03's box by the same 32vh; straight hem; §05's white card straddles the plate's bottom (soft out). §04 still never moves (line 18's still beat stands; its slab window is now .7→.35, whole by grid-top ≤ .46vh). The ONE body flip (line 19) stands. Seam devices are now: climb-over-pin / fade+whitespace / CURTAIN LIFT / ground flip / recede — five, distinct (law 1). (aacb872)
- 2026-09-06 · §03→§04 · the §04 header sits 0.9×--seam-lift below the plate's top so it is the first thing uncovered (p≈.1); the slab has NO fade or rise of its own — the curtain is the only reveal, the ruled line's walk and the demos' one-shots are the section's only motion; §05's card top sits --exit-lap above the plate's bottom edge (a1f006f)
- 2026-09-06 · §04 · header wipes at data-wipe-at .86 — the frame it clears the curtain's hem (b58a76e)
- 2026-09-06 · §03→§04 · SUPERSEDES lines 26–28 on Jake's call ("i really dont like the site changes, revert please", said after seeing the three curtain-lift frames): the CURTAIN LIFT and the LIGHT §04 ROOM are RETIRED — code restored to `40aec08` (§04 on the void, whitespace-only seam of line 8 stands, the ruled slab of line 25 kept). What was rejected on sight = a light room in the middle of the dark room; any future dramatic seam here stays on black.
- 2026-09-07 · §02 · the pictures are PHOTOGRAPHS, not CSS mocks: each stage's real screen was captured, given a drawn iOS status bar in a notch-safe band (`scripts/services/statusbar.py`), and handed to nano_banana_pro at 4k as an `image_references` — the model paints the hardware around a screen it must not redraw (recipe + failure modes in `design-dna/higgsfield/`). The shared `.dvc` / Google / chat skins stay in globals.css for the shipped site's own sections; /dark stopped drawing them.
- 2026-09-07 · §02 · the LANDING stage is a DUO — the desktop build on a laptop with the mobile build on a phone in front of it, both screens real, one generation. It replaced a third phone of the same client's site on Jake's call ("we don't really need the desert wings middle one because we are doing the duo"): the duo already shows that page on two screens, so a phone of it as well was the same evidence twice.
- 2026-09-07 · §02 · the well is the SHOT's own 4:3 on the shot's own dark ground (#1c1a20), not a `clamp()` height on a light #ededf0 tray — a box of a different shape would crop a frame already composed and cropped once, and a light ground under a dark JPEG is a bright hole while it decodes.
- 2026-09-07 · §02 · AMENDS the LENS's LIGHT (the `opacity: .5 + .5·spot` on the whole row, "phones included"): the spot dim now rides `.dr-svc-row > :not(.dr-svc-well)`, and the picture takes the same curve as a dark veil (`.dr-svc-well::after`, #0b0b0d, `opacity: (1 - spot) × .45`). Intent unchanged — mechanism changed because the picture became DARK: opacity fades a thing toward the page, this page is near-white, and the unlit stages rendered at rgb(124,123,126) — pale grey slabs. With the veil the same patch renders rgb(26,25,28). Measured, not guessed.
