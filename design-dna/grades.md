# Grades — the critic's log

Append one entry per critic run, most recent last. Format:

`- YYYY-MM-DD · <section> · <score>/10 · THE ONE CHANGE: <instruction> · <resolved? y/n, builder step + commit if fixed>`

(no entries yet)
